versions:
  - name: Swagger
    fullVersion: '2.0'
    version: '2.0'
    fullName: Swagger 2.0
  - name: OpenAPI
    fullVersion: 3.0.3
    version: '3.0'
    fullName: OpenAPI 3.0
  - name: OpenAPI
    fullVersion: 3.1.0
    version: '3.1'
    fullName: OpenAPI 3.1
schemas:
  - name: Root Object
    isRoot: true
    versions:
      - '2.0'
      - '3.0'
      - '3.1'
    logs:
      - version: '2.0'
        isExtensible: true
        description: <p>This is the root document object for the API specification. It combines what previously was the Resource Listing and API Declaration (version 1.2 and earlier) together into one document.</p>
      - version: '3.0'
        isExtensible: true
        description: <p>This is the root document object of the <a href="#oasDocument">OpenAPI document</a>.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: <p>This is the root object of the <a href="#oasDocument">OpenAPI document</a>.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: <p>This is the root object of the <a href="#oasDocument">OpenAPI document</a>.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Root Object (2.0, 3.0, 3.1)
    fields:
      - name: swagger
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Specifies the Swagger Specification version being used. It can be used by the Swagger UI and other clients to interpret the API listing. The value MUST be <code>"2.0"</code>.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Specifies the Swagger Specification version being used. It can be used by the Swagger UI and other clients to interpret the API listing. The value MUST be <code>"2.0"</code>.
      - name: info
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Info Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Provides metadata about the API. The metadata can be used by the clients if needed.
          - version: '3.0'
            mainType: Info Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: Provides metadata about the API. The metadata MAY be used by tooling as required.
          - version: '3.1'
            mainType: Info Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: Provides metadata about the API. The metadata MAY be used by tooling as required.
        mainType: Info Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: Provides metadata about the API. The metadata MAY be used by tooling as required.
      - name: host
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The host (name or ip) serving the API. This MUST be the host only and does not include the scheme nor sub-paths. It MAY include a port. If the <code>host</code> is not included, the host serving the documentation is to be used (including the port). The <code>host</code> does not support <a href="#pathTemplating">path templating</a>.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The host (name or ip) serving the API. This MUST be the host only and does not include the scheme nor sub-paths. It MAY include a port. If the <code>host</code> is not included, the host serving the documentation is to be used (including the port). The <code>host</code> does not support <a href="#pathTemplating">path templating</a>.
      - name: basePath
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The base path on which the API is served, which is relative to the <a href="#swaggerHost"><code>host</code></a>. If it is not included, the API is served directly under the <code>host</code>. The value MUST start with a leading slash (<code>/</code>). The <code>basePath</code> does not support <a href="#pathTemplating">path templating</a>.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The base path on which the API is served, which is relative to the <a href="#swaggerHost"><code>host</code></a>. If it is not included, the API is served directly under the <code>host</code>. The value MUST start with a leading slash (<code>/</code>). The <code>basePath</code> does not support <a href="#pathTemplating">path templating</a>.
      - name: schemes
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: string
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: 'The transfer protocol of the API. Values MUST be from the list: <code>"http"</code>, <code>"https"</code>, <code>"ws"</code>, <code>"wss"</code>. If the <code>schemes</code> is not included, the default scheme to be used is the one used to access the Swagger definition itself.'
        mainType: string
        parentType: array
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: 'The transfer protocol of the API. Values MUST be from the list: <code>"http"</code>, <code>"https"</code>, <code>"ws"</code>, <code>"wss"</code>. If the <code>schemes</code> is not included, the default scheme to be used is the one used to access the Swagger definition itself.'
      - name: consumes
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: string
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A list of MIME types the APIs can consume. This is global to all APIs but can be overridden on specific API calls. Value MUST be as described under <a href="#mimeTypes">Mime Types</a>.
        mainType: string
        parentType: array
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A list of MIME types the APIs can consume. This is global to all APIs but can be overridden on specific API calls. Value MUST be as described under <a href="#mimeTypes">Mime Types</a>.
      - name: produces
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: string
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A list of MIME types the APIs can produce. This is global to all APIs but can be overridden on specific API calls. Value MUST be as described under <a href="#mimeTypes">Mime Types</a>.
        mainType: string
        parentType: array
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A list of MIME types the APIs can produce. This is global to all APIs but can be overridden on specific API calls. Value MUST be as described under <a href="#mimeTypes">Mime Types</a>.
      - name: paths
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Paths Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The available paths and operations for the API.
          - version: '3.0'
            mainType: Paths Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The available paths and operations for the API.
          - version: '3.1'
            mainType: Paths Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The available paths and operations for the API.
        mainType: Paths Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The available paths and operations for the API.
      - name: definitions
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Definitions Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An object to hold data types produced and consumed by operations.
        mainType: Definitions Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: An object to hold data types produced and consumed by operations.
      - name: parameters
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Parameters Definitions Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An object to hold parameters that can be used across operations. This property <em>does not</em> define global parameters for all operations.
        mainType: Parameters Definitions Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: An object to hold parameters that can be used across operations. This property <em>does not</em> define global parameters for all operations.
      - name: responses
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Responses Definitions Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An object to hold responses that can be used across operations. This property <em>does not</em> define global responses for all operations.
        mainType: Responses Definitions Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: An object to hold responses that can be used across operations. This property <em>does not</em> define global responses for all operations.
      - name: securityDefinitions
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Security Definitions Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Security scheme definitions that can be used across the specification.
        mainType: Security Definitions Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Security scheme definitions that can be used across the specification.
      - name: security
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Security Requirement Object
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A declaration of which security schemes are applied for the API as a whole. The list of values describes alternative security schemes that can be used (that is, there is a logical OR between the security requirements). Individual operations can override this definition.
          - version: '3.0'
            mainType: Security Requirement Object
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A declaration of which security mechanisms can be used across the API. The list of values includes alternative security requirement objects that can be used. Only one of the security requirement objects need to be satisfied to authorize a request. Individual operations can override this definition. To make security optional, an empty security requirement (<code>{}</code>) can be included in the array.
          - version: '3.1'
            mainType: Security Requirement Object
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A declaration of which security mechanisms can be used across the API. The list of values includes alternative security requirement objects that can be used. Only one of the security requirement objects need to be satisfied to authorize a request. Individual operations can override this definition. To make security optional, an empty security requirement (<code>{}</code>) can be included in the array.
        mainType: Security Requirement Object
        parentType: array
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A declaration of which security mechanisms can be used across the API. The list of values includes alternative security requirement objects that can be used. Only one of the security requirement objects need to be satisfied to authorize a request. Individual operations can override this definition. To make security optional, an empty security requirement (<code>{}</code>) can be included in the array.
      - name: tags
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Tag Object
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A list of tags used by the specification with additional metadata. The order of the tags can be used to reflect on their order by the parsing tools. Not all tags that are used by the <a href="#operationObject">Operation Object</a> must be declared. The tags that are not declared may be organized randomly or based on the tools' logic. Each tag name in the list MUST be unique.
          - version: '3.0'
            mainType: Tag Object
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A list of tags used by the specification with additional metadata. The order of the tags can be used to reflect on their order by the parsing tools. Not all tags that are used by the <a href="#operationObject">Operation Object</a> must be declared. The tags that are not declared MAY be organized randomly or based on the tools' logic. Each tag name in the list MUST be unique.
          - version: '3.1'
            mainType: Tag Object
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A list of tags used by the document with additional metadata. The order of the tags can be used to reflect on their order by the parsing tools. Not all tags that are used by the <a href="#operationObject">Operation Object</a> must be declared. The tags that are not declared MAY be organized randomly or based on the tools' logic. Each tag name in the list MUST be unique.
        mainType: Tag Object
        parentType: array
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A list of tags used by the document with additional metadata. The order of the tags can be used to reflect on their order by the parsing tools. Not all tags that are used by the <a href="#operationObject">Operation Object</a> must be declared. The tags that are not declared MAY be organized randomly or based on the tools' logic. Each tag name in the list MUST be unique.
      - name: externalDocs
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: External Documentation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Additional external documentation.
          - version: '3.0'
            mainType: External Documentation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Additional external documentation.
          - version: '3.1'
            mainType: External Documentation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Additional external documentation.
        mainType: External Documentation Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Additional external documentation.
      - name: ^x-
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the Swagger Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. See <a href="#vendorExtensions">Vendor Extensions</a> for further details.
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
      - name: openapi
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: This string MUST be the <a href="https://semver.org/spec/v2.0.0.html">semantic version number</a> of the <a href="#versions">OpenAPI Specification version</a> that the OpenAPI document uses. The <code>openapi</code> field SHOULD be used by tooling specifications and clients to interpret the OpenAPI document. This is <em>not</em> related to the API <a href="#infoVersion"><code>info.version</code></a> string.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: This string MUST be the <a href="#versions">version number</a> of the OpenAPI Specification that the OpenAPI document uses. The <code>openapi</code> field SHOULD be used by tooling to interpret the OpenAPI document. This is <em>not</em> related to the API <a href="#infoVersion"><code>info.version</code></a> string.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: This string MUST be the <a href="#versions">version number</a> of the OpenAPI Specification that the OpenAPI document uses. The <code>openapi</code> field SHOULD be used by tooling to interpret the OpenAPI document. This is <em>not</em> related to the API <a href="#infoVersion"><code>info.version</code></a> string.
      - name: servers
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Server Object
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An array of Server Objects, which provide connectivity information to a target server. If the <code>servers</code> property is not provided, or is an empty array, the default value would be a <a href="#serverObject">Server Object</a> with a <a href="#serverUrl">url</a> value of <code>/</code>.
          - version: '3.1'
            mainType: Server Object
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An array of Server Objects, which provide connectivity information to a target server. If the <code>servers</code> property is not provided, or is an empty array, the default value would be a <a href="#serverObject">Server Object</a> with a <a href="#serverUrl">url</a> value of <code>/</code>.
        mainType: Server Object
        parentType: array
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: An array of Server Objects, which provide connectivity information to a target server. If the <code>servers</code> property is not provided, or is an empty array, the default value would be a <a href="#serverObject">Server Object</a> with a <a href="#serverUrl">url</a> value of <code>/</code>.
      - name: components
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Components Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An element to hold various schemas for the specification.
          - version: '3.1'
            mainType: Components Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An element to hold various schemas for the document.
        mainType: Components Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: An element to hold various schemas for the document.
      - name: jsonSchemaDialect
        versions:
          - '3.1'
        logs:
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The default value for the <code>$schema</code> keyword within <a href="#schemaObject">Schema Objects</a> contained within this OAS document. This MUST be in the form of a URI.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The default value for the <code>$schema</code> keyword within <a href="#schemaObject">Schema Objects</a> contained within this OAS document. This MUST be in the form of a URI.
      - name: webhooks
        versions:
          - '3.1'
        logs:
          - version: '3.1'
            mainType: Path Item Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: The incoming webhooks that MAY be received as part of this API and that the API consumer MAY choose to implement. Closely related to the <code>callbacks</code> feature, this section describes requests initiated other than by an API call, for example by an out of band registration. The key name is a unique string to refer to each webhook, while the (optionally referenced) Path Item Object describes a request that may be initiated by the API provider and the expected responses. An <a href="../examples/v3.1/webhook-example.yaml">example</a> is available.
        mainType: Path Item Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: true
        description: The incoming webhooks that MAY be received as part of this API and that the API consumer MAY choose to implement. Closely related to the <code>callbacks</code> feature, this section describes requests initiated other than by an API call, for example by an out of band registration. The key name is a unique string to refer to each webhook, while the (optionally referenced) Path Item Object describes a request that may be initiated by the API provider and the expected responses. An <a href="../examples/v3.1/webhook-example.yaml">example</a> is available.
  - name: Info Object
    isRoot: false
    versions:
      - '2.0'
      - '3.0'
      - '3.1'
    logs:
      - version: '2.0'
        isExtensible: true
        description: <p>The object provides metadata about the API. The metadata can be used by the clients if needed, and can be presented in the Swagger-UI for convenience.</p>
      - version: '3.0'
        isExtensible: true
        description: |-
          <p>The object provides metadata about the API.
          The metadata MAY be used by the clients if needed, and MAY be presented in editing or documentation generation tools for convenience.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: |-
          <p>The object provides metadata about the API.
          The metadata MAY be used by the clients if needed, and MAY be presented in editing or documentation generation tools for convenience.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: |-
      <p>The object provides metadata about the API.
      The metadata MAY be used by the clients if needed, and MAY be presented in editing or documentation generation tools for convenience.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Info Object (2.0, 3.0, 3.1)
    fields:
      - name: title
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The title of the application.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The title of the API.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The title of the API.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: The title of the API.
      - name: description
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A short description of the application.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A short description of the API.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A description of the API.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A description of the API.
      - name: termsOfService
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The Terms of Service for the API.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A URL to the Terms of Service for the API. MUST be in the format of a URL.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A URL to the Terms of Service for the API. This MUST be in the form of a URL.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A URL to the Terms of Service for the API. This MUST be in the form of a URL.
      - name: contact
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Contact Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The contact information for the exposed API.
          - version: '3.0'
            mainType: Contact Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The contact information for the exposed API.
          - version: '3.1'
            mainType: Contact Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The contact information for the exposed API.
        mainType: Contact Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The contact information for the exposed API.
      - name: license
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: License Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The license information for the exposed API.
          - version: '3.0'
            mainType: License Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The license information for the exposed API.
          - version: '3.1'
            mainType: License Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The license information for the exposed API.
        mainType: License Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The license information for the exposed API.
      - name: version
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Provides the version of the application API (not to be confused with the specification version).
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The version of the OpenAPI document (which is distinct from the <a href="#oasVersion">OpenAPI Specification version</a> or the API implementation version).
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The version of the OpenAPI document (which is distinct from the <a href="#oasVersion">OpenAPI Specification version</a> or the API implementation version).
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: The version of the OpenAPI document (which is distinct from the <a href="#oasVersion">OpenAPI Specification version</a> or the API implementation version).
      - name: ^x-
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the Swagger Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. See <a href="#vendorExtensions">Vendor Extensions</a> for further details.
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
      - name: summary
        versions:
          - '3.1'
        logs:
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A short summary of the API.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A short summary of the API.
  - name: Contact Object
    isRoot: false
    versions:
      - '2.0'
      - '3.0'
      - '3.1'
    logs:
      - version: '2.0'
        isExtensible: true
        description: <p>Contact information for the exposed API.</p>
      - version: '3.0'
        isExtensible: true
        description: <p>Contact information for the exposed API.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: <p>Contact information for the exposed API.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: <p>Contact information for the exposed API.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Contact Object (2.0, 3.0, 3.1)
    fields:
      - name: name
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The identifying name of the contact person/organization.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The identifying name of the contact person/organization.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The identifying name of the contact person/organization.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The identifying name of the contact person/organization.
      - name: url
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The URL pointing to the contact information. MUST be in the format of a URL.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The URL pointing to the contact information. MUST be in the format of a URL.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The URL pointing to the contact information. This MUST be in the form of a URL.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The URL pointing to the contact information. This MUST be in the form of a URL.
      - name: email
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The email address of the contact person/organization. MUST be in the format of an email address.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The email address of the contact person/organization. MUST be in the format of an email address.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The email address of the contact person/organization. This MUST be in the form of an email address.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The email address of the contact person/organization. This MUST be in the form of an email address.
      - name: ^x-
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the Swagger Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. See <a href="#vendorExtensions">Vendor Extensions</a> for further details.
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
  - name: License Object
    isRoot: false
    versions:
      - '2.0'
      - '3.0'
      - '3.1'
    logs:
      - version: '2.0'
        isExtensible: true
        description: <p>License information for the exposed API.</p>
      - version: '3.0'
        isExtensible: true
        description: <p>License information for the exposed API.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: <p>License information for the exposed API.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: <p>License information for the exposed API.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: License Object (2.0, 3.0, 3.1)
    fields:
      - name: name
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The license name used for the API.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The license name used for the API.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The license name used for the API.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: The license name used for the API.
      - name: url
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A URL to the license used for the API. MUST be in the format of a URL.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A URL to the license used for the API. MUST be in the format of a URL.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A URL to the license used for the API. This MUST be in the form of a URL. The <code>url</code> field is mutually exclusive of the <code>identifier</code> field.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A URL to the license used for the API. This MUST be in the form of a URL. The <code>url</code> field is mutually exclusive of the <code>identifier</code> field.
      - name: ^x-
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the Swagger Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. See <a href="#vendorExtensions">Vendor Extensions</a> for further details.
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
      - name: identifier
        versions:
          - '3.1'
        logs:
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An <a href="https://spdx.org/spdx-specification-21-web-version#h.jxpfx0ykyb60">SPDX</a> license expression for the API. The <code>identifier</code> field is mutually exclusive of the <code>url</code> field.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: An <a href="https://spdx.org/spdx-specification-21-web-version#h.jxpfx0ykyb60">SPDX</a> license expression for the API. The <code>identifier</code> field is mutually exclusive of the <code>url</code> field.
  - name: Paths Object
    isRoot: false
    versions:
      - '2.0'
      - '3.0'
      - '3.1'
    logs:
      - version: '2.0'
        isExtensible: true
        description: |-
          <p>Holds the relative paths to the individual endpoints. The path is appended to the <a href="#swaggerBasePath"><code>basePath</code></a> in order to construct the full URL.
          The Paths may be empty, due to <a href="#securityFiltering">ACL constraints</a>.</p>
      - version: '3.0'
        isExtensible: true
        description: |-
          <p>Holds the relative paths to the individual endpoints and their operations.
          The path is appended to the URL from the <a href="#serverObject"><code>Server Object</code></a> in order to construct the full URL.  The Paths MAY be empty, due to <a href="#securityFiltering">ACL constraints</a>.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: |-
          <p>Holds the relative paths to the individual endpoints and their operations.
          The path is appended to the URL from the <a href="#serverObject"><code>Server Object</code></a> in order to construct the full URL.  The Paths MAY be empty, due to <a href="#securityFiltering">Access Control List (ACL) constraints</a>.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: |-
      <p>Holds the relative paths to the individual endpoints and their operations.
      The path is appended to the URL from the <a href="#serverObject"><code>Server Object</code></a> in order to construct the full URL.  The Paths MAY be empty, due to <a href="#securityFiltering">Access Control List (ACL) constraints</a>.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Paths Object (2.0, 3.0, 3.1)
    fields:
      - name: /{path}
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Path Item Object
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: A relative path to an individual endpoint. The field name MUST begin with a slash. The path is appended to the <a href="#swaggerBasePath"><code>basePath</code></a> in order to construct the full URL. <a href="#pathTemplating">Path templating</a> is allowed.
          - version: '3.0'
            mainType: Path Item Object
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: A relative path to an individual endpoint. The field name MUST begin with a forward slash (<code>/</code>). The path is <strong>appended</strong> (no relative URL resolution) to the expanded URL from the <a href="#serverObject"><code>Server Object</code></a>'s <code>url</code> field in order to construct the full URL. <a href="#pathTemplating">Path templating</a> is allowed. When matching URLs, concrete (non-templated) paths would be matched before their templated counterparts. Templated paths with the same hierarchy but different templated names MUST NOT exist as they are identical. In case of ambiguous matching, it's up to the tooling to decide which one to use.
          - version: '3.1'
            mainType: Path Item Object
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: A relative path to an individual endpoint. The field name MUST begin with a forward slash (<code>/</code>). The path is <strong>appended</strong> (no relative URL resolution) to the expanded URL from the <a href="#serverObject"><code>Server Object</code></a>'s <code>url</code> field in order to construct the full URL. <a href="#pathTemplating">Path templating</a> is allowed. When matching URLs, concrete (non-templated) paths would be matched before their templated counterparts. Templated paths with the same hierarchy but different templated names MUST NOT exist as they are identical. In case of ambiguous matching, it's up to the tooling to decide which one to use.
        mainType: Path Item Object
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: A relative path to an individual endpoint. The field name MUST begin with a forward slash (<code>/</code>). The path is <strong>appended</strong> (no relative URL resolution) to the expanded URL from the <a href="#serverObject"><code>Server Object</code></a>'s <code>url</code> field in order to construct the full URL. <a href="#pathTemplating">Path templating</a> is allowed. When matching URLs, concrete (non-templated) paths would be matched before their templated counterparts. Templated paths with the same hierarchy but different templated names MUST NOT exist as they are identical. In case of ambiguous matching, it's up to the tooling to decide which one to use.
      - name: ^x-
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the Swagger Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. See <a href="#vendorExtensions">Vendor Extensions</a> for further details.
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
  - name: Path Item Object
    isRoot: false
    versions:
      - '2.0'
      - '3.0'
      - '3.1'
    logs:
      - version: '2.0'
        isExtensible: true
        description: |-
          <p>Describes the operations available on a single path.
          A Path Item may be empty, due to <a href="#securityFiltering">ACL constraints</a>. The path itself is still exposed to the documentation viewer but they will not know which operations and parameters are available.</p>
      - version: '3.0'
        isExtensible: true
        description: |-
          <p>Describes the operations available on a single path.
          A Path Item MAY be empty, due to <a href="#securityFiltering">ACL constraints</a>.
          The path itself is still exposed to the documentation viewer but they will not know which operations and parameters are available.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: |-
          <p>Describes the operations available on a single path.
          A Path Item MAY be empty, due to <a href="#securityFiltering">ACL constraints</a>.
          The path itself is still exposed to the documentation viewer but they will not know which operations and parameters are available.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: |-
      <p>Describes the operations available on a single path.
      A Path Item MAY be empty, due to <a href="#securityFiltering">ACL constraints</a>.
      The path itself is still exposed to the documentation viewer but they will not know which operations and parameters are available.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Path Item Object (2.0, 3.0, 3.1)
    fields:
      - name: $ref
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows for an external definition of this path item. The referenced structure MUST be in the format of a <a href="#pathItemObject">Path Item Object</a>. If there are conflicts between the referenced definition and this Path Item's definition, the behavior is <em>undefined</em>.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows for an external definition of this path item. The referenced structure MUST be in the format of a <a href="#pathItemObject">Path Item Object</a>.  In case a Path Item Object field appears both in the defined object and the referenced object, the behavior is undefined.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows for a referenced definition of this path item. The referenced structure MUST be in the form of a <a href="#pathItemObject">Path Item Object</a>.  In case a Path Item Object field appears both in the defined object and the referenced object, the behavior is undefined. See the rules for resolving <a href="#relativeReferencesURI">Relative References</a>.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows for a referenced definition of this path item. The referenced structure MUST be in the form of a <a href="#pathItemObject">Path Item Object</a>.  In case a Path Item Object field appears both in the defined object and the referenced object, the behavior is undefined. See the rules for resolving <a href="#relativeReferencesURI">Relative References</a>.
      - name: get
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a GET operation on this path.
          - version: '3.0'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a GET operation on this path.
          - version: '3.1'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a GET operation on this path.
        mainType: Operation Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A definition of a GET operation on this path.
      - name: put
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a PUT operation on this path.
          - version: '3.0'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a PUT operation on this path.
          - version: '3.1'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a PUT operation on this path.
        mainType: Operation Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A definition of a PUT operation on this path.
      - name: post
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a POST operation on this path.
          - version: '3.0'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a POST operation on this path.
          - version: '3.1'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a POST operation on this path.
        mainType: Operation Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A definition of a POST operation on this path.
      - name: delete
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a DELETE operation on this path.
          - version: '3.0'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a DELETE operation on this path.
          - version: '3.1'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a DELETE operation on this path.
        mainType: Operation Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A definition of a DELETE operation on this path.
      - name: options
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a OPTIONS operation on this path.
          - version: '3.0'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a OPTIONS operation on this path.
          - version: '3.1'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a OPTIONS operation on this path.
        mainType: Operation Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A definition of a OPTIONS operation on this path.
      - name: head
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a HEAD operation on this path.
          - version: '3.0'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a HEAD operation on this path.
          - version: '3.1'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a HEAD operation on this path.
        mainType: Operation Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A definition of a HEAD operation on this path.
      - name: patch
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a PATCH operation on this path.
          - version: '3.0'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a PATCH operation on this path.
          - version: '3.1'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a PATCH operation on this path.
        mainType: Operation Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A definition of a PATCH operation on this path.
      - name: parameters
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Parameter Object
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: A list of parameters that are applicable for all the operations described under this path. These parameters can be overridden at the operation level, but cannot be removed there. The list MUST NOT include duplicated parameters. A unique parameter is defined by a combination of a <a href="#parameterName">name</a> and <a href="#parameterIn">location</a>. The list can use the <a href="#referenceObject">Reference Object</a> to link to parameters that are defined at the <a href="#swaggerParameters">Swagger Object's parameters</a>. There can be one "body" parameter at most.
          - version: '3.0'
            mainType: Parameter Object
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: A list of parameters that are applicable for all the operations described under this path. These parameters can be overridden at the operation level, but cannot be removed there. The list MUST NOT include duplicated parameters. A unique parameter is defined by a combination of a <a href="#parameterName">name</a> and <a href="#parameterIn">location</a>. The list can use the <a href="#referenceObject">Reference Object</a> to link to parameters that are defined at the <a href="#componentsParameters">OpenAPI Object's components/parameters</a>.
          - version: '3.1'
            mainType: Parameter Object
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: A list of parameters that are applicable for all the operations described under this path. These parameters can be overridden at the operation level, but cannot be removed there. The list MUST NOT include duplicated parameters. A unique parameter is defined by a combination of a <a href="#parameterName">name</a> and <a href="#parameterIn">location</a>. The list can use the <a href="#referenceObject">Reference Object</a> to link to parameters that are defined at the <a href="#componentsParameters">OpenAPI Object's components/parameters</a>.
        mainType: Parameter Object
        parentType: array
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: true
        description: A list of parameters that are applicable for all the operations described under this path. These parameters can be overridden at the operation level, but cannot be removed there. The list MUST NOT include duplicated parameters. A unique parameter is defined by a combination of a <a href="#parameterName">name</a> and <a href="#parameterIn">location</a>. The list can use the <a href="#referenceObject">Reference Object</a> to link to parameters that are defined at the <a href="#componentsParameters">OpenAPI Object's components/parameters</a>.
      - name: ^x-
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the Swagger Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. See <a href="#vendorExtensions">Vendor Extensions</a> for further details.
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
      - name: summary
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An optional, string summary, intended to apply to all operations in this path.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An optional, string summary, intended to apply to all operations in this path.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: An optional, string summary, intended to apply to all operations in this path.
      - name: description
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An optional, string description, intended to apply to all operations in this path.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An optional, string description, intended to apply to all operations in this path.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: An optional, string description, intended to apply to all operations in this path.
      - name: trace
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a TRACE operation on this path.
          - version: '3.1'
            mainType: Operation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of a TRACE operation on this path.
        mainType: Operation Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A definition of a TRACE operation on this path.
      - name: servers
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Server Object
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An alternative <code>server</code> array to service all operations in this path.
          - version: '3.1'
            mainType: Server Object
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An alternative <code>server</code> array to service all operations in this path.
        mainType: Server Object
        parentType: array
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: An alternative <code>server</code> array to service all operations in this path.
  - name: Operation Object
    isRoot: false
    versions:
      - '2.0'
      - '3.0'
      - '3.1'
    logs:
      - version: '2.0'
        isExtensible: true
        description: <p>Describes a single API operation on a path.</p>
      - version: '3.0'
        isExtensible: true
        description: <p>Describes a single API operation on a path.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: <p>Describes a single API operation on a path.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: <p>Describes a single API operation on a path.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Operation Object (2.0, 3.0, 3.1)
    fields:
      - name: tags
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A list of tags for API documentation control. Tags can be used for logical grouping of operations by resources or any other qualifier.
          - version: '3.0'
            mainType: string
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A list of tags for API documentation control. Tags can be used for logical grouping of operations by resources or any other qualifier.
          - version: '3.1'
            mainType: string
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A list of tags for API documentation control. Tags can be used for logical grouping of operations by resources or any other qualifier.
        mainType: string
        parentType: array
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A list of tags for API documentation control. Tags can be used for logical grouping of operations by resources or any other qualifier.
      - name: summary
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A short summary of what the operation does. For maximum readability in the swagger-ui, this field SHOULD be less than 120 characters.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A short summary of what the operation does.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A short summary of what the operation does.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A short summary of what the operation does.
      - name: description
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A verbose explanation of the operation behavior.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A verbose explanation of the operation behavior.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A verbose explanation of the operation behavior.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A verbose explanation of the operation behavior.
      - name: externalDocs
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: External Documentation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Additional external documentation for this operation.
          - version: '3.0'
            mainType: External Documentation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Additional external documentation for this operation.
          - version: '3.1'
            mainType: External Documentation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Additional external documentation for this operation.
        mainType: External Documentation Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Additional external documentation for this operation.
      - name: operationId
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Unique string used to identify the operation. The id MUST be unique among all operations described in the API. Tools and libraries MAY use the operationId to uniquely identify an operation, therefore, it is recommended to follow common programming naming conventions.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Unique string used to identify the operation. The id MUST be unique among all operations described in the API. The operationId value is <strong>case-sensitive</strong>. Tools and libraries MAY use the operationId to uniquely identify an operation, therefore, it is RECOMMENDED to follow common programming naming conventions.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Unique string used to identify the operation. The id MUST be unique among all operations described in the API. The operationId value is <strong>case-sensitive</strong>. Tools and libraries MAY use the operationId to uniquely identify an operation, therefore, it is RECOMMENDED to follow common programming naming conventions.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Unique string used to identify the operation. The id MUST be unique among all operations described in the API. The operationId value is <strong>case-sensitive</strong>. Tools and libraries MAY use the operationId to uniquely identify an operation, therefore, it is RECOMMENDED to follow common programming naming conventions.
      - name: consumes
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: string
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A list of MIME types the operation can consume. This overrides the <a href="#swaggerConsumes"><code>consumes</code></a> definition at the Swagger Object. An empty value MAY be used to clear the global definition. Value MUST be as described under <a href="#mimeTypes">Mime Types</a>.
        mainType: string
        parentType: array
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A list of MIME types the operation can consume. This overrides the <a href="#swaggerConsumes"><code>consumes</code></a> definition at the Swagger Object. An empty value MAY be used to clear the global definition. Value MUST be as described under <a href="#mimeTypes">Mime Types</a>.
      - name: produces
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: string
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A list of MIME types the operation can produce. This overrides the <a href="#swaggerProduces"><code>produces</code></a> definition at the Swagger Object. An empty value MAY be used to clear the global definition. Value MUST be as described under <a href="#mimeTypes">Mime Types</a>.
        mainType: string
        parentType: array
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A list of MIME types the operation can produce. This overrides the <a href="#swaggerProduces"><code>produces</code></a> definition at the Swagger Object. An empty value MAY be used to clear the global definition. Value MUST be as described under <a href="#mimeTypes">Mime Types</a>.
      - name: parameters
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Parameter Object
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: A list of parameters that are applicable for this operation. If a parameter is already defined at the <a href="#pathItemParameters">Path Item</a>, the new definition will override it, but can never remove it. The list MUST NOT include duplicated parameters. A unique parameter is defined by a combination of a <a href="#parameterName">name</a> and <a href="#parameterIn">location</a>. The list can use the <a href="#referenceObject">Reference Object</a> to link to parameters that are defined at the <a href="#swaggerParameters">Swagger Object's parameters</a>. There can be one "body" parameter at most.
          - version: '3.0'
            mainType: Parameter Object
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: A list of parameters that are applicable for this operation. If a parameter is already defined at the <a href="#pathItemParameters">Path Item</a>, the new definition will override it but can never remove it. The list MUST NOT include duplicated parameters. A unique parameter is defined by a combination of a <a href="#parameterName">name</a> and <a href="#parameterIn">location</a>. The list can use the <a href="#referenceObject">Reference Object</a> to link to parameters that are defined at the <a href="#componentsParameters">OpenAPI Object's components/parameters</a>.
          - version: '3.1'
            mainType: Parameter Object
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: A list of parameters that are applicable for this operation. If a parameter is already defined at the <a href="#pathItemParameters">Path Item</a>, the new definition will override it but can never remove it. The list MUST NOT include duplicated parameters. A unique parameter is defined by a combination of a <a href="#parameterName">name</a> and <a href="#parameterIn">location</a>. The list can use the <a href="#referenceObject">Reference Object</a> to link to parameters that are defined at the <a href="#componentsParameters">OpenAPI Object's components/parameters</a>.
        mainType: Parameter Object
        parentType: array
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: true
        description: A list of parameters that are applicable for this operation. If a parameter is already defined at the <a href="#pathItemParameters">Path Item</a>, the new definition will override it but can never remove it. The list MUST NOT include duplicated parameters. A unique parameter is defined by a combination of a <a href="#parameterName">name</a> and <a href="#parameterIn">location</a>. The list can use the <a href="#referenceObject">Reference Object</a> to link to parameters that are defined at the <a href="#componentsParameters">OpenAPI Object's components/parameters</a>.
      - name: responses
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Responses Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The list of possible responses as they are returned from executing this operation.
          - version: '3.0'
            mainType: Responses Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The list of possible responses as they are returned from executing this operation.
          - version: '3.1'
            mainType: Responses Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The list of possible responses as they are returned from executing this operation.
        mainType: Responses Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The list of possible responses as they are returned from executing this operation.
      - name: schemes
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: string
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: 'The transfer protocol for the operation. Values MUST be from the list: <code>"http"</code>, <code>"https"</code>, <code>"ws"</code>, <code>"wss"</code>. The value overrides the Swagger Object <a href="#swaggerSchemes"><code>schemes</code></a> definition.'
        mainType: string
        parentType: array
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: 'The transfer protocol for the operation. Values MUST be from the list: <code>"http"</code>, <code>"https"</code>, <code>"ws"</code>, <code>"wss"</code>. The value overrides the Swagger Object <a href="#swaggerSchemes"><code>schemes</code></a> definition.'
      - name: deprecated
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Declares this operation to be deprecated. Usage of the declared operation should be refrained. Default value is <code>false</code>.
          - version: '3.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Declares this operation to be deprecated. Consumers SHOULD refrain from usage of the declared operation. Default value is <code>false</code>.
          - version: '3.1'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Declares this operation to be deprecated. Consumers SHOULD refrain from usage of the declared operation. Default value is <code>false</code>.
        mainType: boolean
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Declares this operation to be deprecated. Consumers SHOULD refrain from usage of the declared operation. Default value is <code>false</code>.
      - name: security
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Security Requirement Object
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A declaration of which security schemes are applied for this operation. The list of values describes alternative security schemes that can be used (that is, there is a logical OR between the security requirements). This definition overrides any declared top-level <a href="#swaggerSecurity"><code>security</code></a>. To remove a top-level security declaration, an empty array can be used.
          - version: '3.0'
            mainType: Security Requirement Object
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A declaration of which security mechanisms can be used for this operation. The list of values includes alternative security requirement objects that can be used. Only one of the security requirement objects need to be satisfied to authorize a request. To make security optional, an empty security requirement (<code>{}</code>) can be included in the array. This definition overrides any declared top-level <a href="#oasSecurity"><code>security</code></a>. To remove a top-level security declaration, an empty array can be used.
          - version: '3.1'
            mainType: Security Requirement Object
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A declaration of which security mechanisms can be used for this operation. The list of values includes alternative security requirement objects that can be used. Only one of the security requirement objects need to be satisfied to authorize a request. To make security optional, an empty security requirement (<code>{}</code>) can be included in the array. This definition overrides any declared top-level <a href="#oasSecurity"><code>security</code></a>. To remove a top-level security declaration, an empty array can be used.
        mainType: Security Requirement Object
        parentType: array
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A declaration of which security mechanisms can be used for this operation. The list of values includes alternative security requirement objects that can be used. Only one of the security requirement objects need to be satisfied to authorize a request. To make security optional, an empty security requirement (<code>{}</code>) can be included in the array. This definition overrides any declared top-level <a href="#oasSecurity"><code>security</code></a>. To remove a top-level security declaration, an empty array can be used.
      - name: ^x-
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the Swagger Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. See <a href="#vendorExtensions">Vendor Extensions</a> for further details.
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
      - name: requestBody
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Request Body Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: The request body applicable for this operation.  The <code>requestBody</code> is only supported in HTTP methods where the HTTP 1.1 specification <a href="https://tools.ietf.org/html/rfc7231#section-4.3.1">RFC7231</a> has explicitly defined semantics for request bodies.  In other cases where the HTTP spec is vague, <code>requestBody</code> SHALL be ignored by consumers.
          - version: '3.1'
            mainType: Request Body Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: The request body applicable for this operation.  The <code>requestBody</code> is fully supported in HTTP methods where the HTTP 1.1 specification <a href="https://tools.ietf.org/html/rfc7231#section-4.3.1">RFC7231</a> has explicitly defined semantics for request bodies.  In other cases where the HTTP spec is vague (such as <a href="https://tools.ietf.org/html/rfc7231#section-4.3.1">GET</a>, <a href="https://tools.ietf.org/html/rfc7231#section-4.3.2">HEAD</a> and <a href="https://tools.ietf.org/html/rfc7231#section-4.3.5">DELETE</a>), <code>requestBody</code> is permitted but does not have well-defined semantics and SHOULD be avoided if possible.
        mainType: Request Body Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: true
        description: The request body applicable for this operation.  The <code>requestBody</code> is fully supported in HTTP methods where the HTTP 1.1 specification <a href="https://tools.ietf.org/html/rfc7231#section-4.3.1">RFC7231</a> has explicitly defined semantics for request bodies.  In other cases where the HTTP spec is vague (such as <a href="https://tools.ietf.org/html/rfc7231#section-4.3.1">GET</a>, <a href="https://tools.ietf.org/html/rfc7231#section-4.3.2">HEAD</a> and <a href="https://tools.ietf.org/html/rfc7231#section-4.3.5">DELETE</a>), <code>requestBody</code> is permitted but does not have well-defined semantics and SHOULD be avoided if possible.
      - name: callbacks
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Callback Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: A map of possible out-of band callbacks related to the parent operation. The key is a unique identifier for the Callback Object. Each value in the map is a <a href="#callbackObject">Callback Object</a> that describes a request that may be initiated by the API provider and the expected responses.
          - version: '3.1'
            mainType: Callback Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: A map of possible out-of band callbacks related to the parent operation. The key is a unique identifier for the Callback Object. Each value in the map is a <a href="#callbackObject">Callback Object</a> that describes a request that may be initiated by the API provider and the expected responses.
        mainType: Callback Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: true
        description: A map of possible out-of band callbacks related to the parent operation. The key is a unique identifier for the Callback Object. Each value in the map is a <a href="#callbackObject">Callback Object</a> that describes a request that may be initiated by the API provider and the expected responses.
      - name: servers
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Server Object
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An alternative <code>server</code> array to service this operation. If an alternative <code>server</code> object is specified at the Path Item Object or Root level, it will be overridden by this value.
          - version: '3.1'
            mainType: Server Object
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An alternative <code>server</code> array to service this operation. If an alternative <code>server</code> object is specified at the Path Item Object or Root level, it will be overridden by this value.
        mainType: Server Object
        parentType: array
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: An alternative <code>server</code> array to service this operation. If an alternative <code>server</code> object is specified at the Path Item Object or Root level, it will be overridden by this value.
  - name: External Documentation Object
    isRoot: false
    versions:
      - '2.0'
      - '3.0'
      - '3.1'
    logs:
      - version: '2.0'
        isExtensible: true
        description: <p>Allows referencing an external resource for extended documentation.</p>
      - version: '3.0'
        isExtensible: true
        description: <p>Allows referencing an external resource for extended documentation.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: <p>Allows referencing an external resource for extended documentation.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: <p>Allows referencing an external resource for extended documentation.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: External Documentation Object (2.0, 3.0, 3.1)
    fields:
      - name: description
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A short description of the target documentation.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A short description of the target documentation.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A description of the target documentation.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A description of the target documentation.
      - name: url
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The URL for the target documentation. Value MUST be in the format of a URL.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The URL for the target documentation. Value MUST be in the format of a URL.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The URL for the target documentation. This MUST be in the form of a URL.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: The URL for the target documentation. This MUST be in the form of a URL.
      - name: ^x-
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the Swagger Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. See <a href="#vendorExtensions">Vendor Extensions</a> for further details.
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
  - name: Parameter Object
    isRoot: false
    versions:
      - '2.0'
      - '3.0'
      - '3.1'
    logs:
      - version: '2.0'
        isExtensible: true
        description: |-
          <p>Describes a single operation parameter.</p><p>A unique parameter is defined by a combination of a <a href="#parameterName">name</a> and <a href="#parameterIn">location</a>.</p><p>There are five possible parameter types.
          * Path - Used together with <a href="#pathTemplating">Path Templating</a>, where the parameter value is actually part of the operation's URL. This does not include the host or base path of the API. For example, in <code>/items/{itemId}</code>, the path parameter is <code>itemId</code>.
          * Query - Parameters that are appended to the URL. For example, in <code>/items?id=###</code>, the query parameter is <code>id</code>.
          * Header - Custom headers that are expected as part of the request.
          * Body - The payload that's appended to the HTTP request. Since there can only be one payload, there can only be <em>one</em> body parameter. The name of the body parameter has no effect on the parameter itself and is used for documentation purposes only. Since Form parameters are also in the payload, body and form parameters cannot exist together for the same operation.
          * Form - Used to describe the payload of an HTTP request when either <code>application/x-www-form-urlencoded</code>, <code>multipart/form-data</code> or both are used as the content type of the request (in Swagger's definition, the <a href="#operationConsumes"><code>consumes</code></a> property of an operation). This is the only parameter type that can be used to send files, thus supporting the <code>file</code> type. Since form parameters are sent in the payload, they cannot be declared together with a body parameter for the same operation. Form parameters have a different format based on the content-type used (for further details, consult http://www.w3.org/TR/html401/interact/forms.html#h-17.13.4):
            * <code>application/x-www-form-urlencoded</code> - Similar to the format of Query parameters but as a payload. For example, <code>foo=1&amp;bar=swagger</code> - both <code>foo</code> and <code>bar</code> are form parameters. This is normally used for simple parameters that are being transferred.
            * <code>multipart/form-data</code> - each parameter takes a section in the payload with an internal header. For example, for the header <code>Content-Disposition: form-data; name="submit-name"</code> the name of the parameter is <code>submit-name</code>. This type of form parameters is more commonly used for file transfers.</p>
      - version: '3.0'
        isExtensible: true
        description: <p>Describes a single operation parameter.</p><p>A unique parameter is defined by a combination of a <a href="#parameterName">name</a> and <a href="#parameterIn">location</a>.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: <p>Describes a single operation parameter.</p><p>A unique parameter is defined by a combination of a <a href="#parameterName">name</a> and <a href="#parameterIn">location</a>.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: <p>Describes a single operation parameter.</p><p>A unique parameter is defined by a combination of a <a href="#parameterName">name</a> and <a href="#parameterIn">location</a>.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Parameter Object (2.0, 3.0, 3.1)
    fields:
      - name: name
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The name of the parameter. Parameter names are <em>case sensitive</em>. <ul><li>If <a href="#parameterIn"><code>in</code></a> is <code>"path"</code>, the <code>name</code> field MUST correspond to the associated path segment from the <a href="#pathsPath">path</a> field in the <a href="#pathsObject">Paths Object</a>. See <a href="#pathTemplating">Path Templating</a> for further information.<li>For all other cases, the <code>name</code> corresponds to the parameter name used based on the <a href="#parameterIn"><code>in</code></a> property.</li></li></ul>
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The name of the parameter. Parameter names are <em>case sensitive</em>. <ul><li>If <a href="#parameterIn"><code>in</code></a> is <code>"path"</code>, the <code>name</code> field MUST correspond to a template expression occurring within the <a href="#pathsPath">path</a> field in the <a href="#pathsObject">Paths Object</a>. See <a href="#pathTemplating">Path Templating</a> for further information.<li>If <a href="#parameterIn"><code>in</code></a> is <code>"header"</code> and the <code>name</code> field is <code>"Accept"</code>, <code>"Content-Type"</code> or <code>"Authorization"</code>, the parameter definition SHALL be ignored.<li>For all other cases, the <code>name</code> corresponds to the parameter name used by the <a href="#parameterIn"><code>in</code></a> property.</li></li></li></ul>
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The name of the parameter. Parameter names are <em>case sensitive</em>. <ul><li>If <a href="#parameterIn"><code>in</code></a> is <code>"path"</code>, the <code>name</code> field MUST correspond to a template expression occurring within the <a href="#pathsPath">path</a> field in the <a href="#pathsObject">Paths Object</a>. See <a href="#pathTemplating">Path Templating</a> for further information.<li>If <a href="#parameterIn"><code>in</code></a> is <code>"header"</code> and the <code>name</code> field is <code>"Accept"</code>, <code>"Content-Type"</code> or <code>"Authorization"</code>, the parameter definition SHALL be ignored.<li>For all other cases, the <code>name</code> corresponds to the parameter name used by the <a href="#parameterIn"><code>in</code></a> property.</li></li></li></ul>
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: The name of the parameter. Parameter names are <em>case sensitive</em>. <ul><li>If <a href="#parameterIn"><code>in</code></a> is <code>"path"</code>, the <code>name</code> field MUST correspond to a template expression occurring within the <a href="#pathsPath">path</a> field in the <a href="#pathsObject">Paths Object</a>. See <a href="#pathTemplating">Path Templating</a> for further information.<li>If <a href="#parameterIn"><code>in</code></a> is <code>"header"</code> and the <code>name</code> field is <code>"Accept"</code>, <code>"Content-Type"</code> or <code>"Authorization"</code>, the parameter definition SHALL be ignored.<li>For all other cases, the <code>name</code> corresponds to the parameter name used by the <a href="#parameterIn"><code>in</code></a> property.</li></li></li></ul>
      - name: in
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The location of the parameter. Possible values are "query", "header", "path", "formData" or "body".
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The location of the parameter. Possible values are <code>"query"</code>, <code>"header"</code>, <code>"path"</code> or <code>"cookie"</code>.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The location of the parameter. Possible values are <code>"query"</code>, <code>"header"</code>, <code>"path"</code> or <code>"cookie"</code>.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: The location of the parameter. Possible values are <code>"query"</code>, <code>"header"</code>, <code>"path"</code> or <code>"cookie"</code>.
      - name: description
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A brief description of the parameter.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A brief description of the parameter.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A brief description of the parameter.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A brief description of the parameter.
      - name: required
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Determines whether this parameter is mandatory. If the parameter is <a href="#parameterIn"><code>in</code></a> "path", this property is <strong>required</strong> and its value MUST be <code>true</code>. Otherwise, the property MAY be included and its default value is <code>false</code>.
          - version: '3.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Determines whether this parameter is mandatory. If the <a href="#parameterIn">parameter location</a> is <code>"path"</code>, this property is <strong>REQUIRED</strong> and its value MUST be <code>true</code>. Otherwise, the property MAY be included and its default value is <code>false</code>.
          - version: '3.1'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Determines whether this parameter is mandatory. If the <a href="#parameterIn">parameter location</a> is <code>"path"</code>, this property is <strong>REQUIRED</strong> and its value MUST be <code>true</code>. Otherwise, the property MAY be included and its default value is <code>false</code>.
        mainType: boolean
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Determines whether this parameter is mandatory. If the <a href="#parameterIn">parameter location</a> is <code>"path"</code>, this property is <strong>REQUIRED</strong> and its value MUST be <code>true</code>. Otherwise, the property MAY be included and its default value is <code>false</code>.
      - name: schema
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Schema Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The schema defining the type used for the body parameter.
          - version: '3.0'
            mainType: Schema Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: The schema defining the type used for the parameter.
          - version: '3.1'
            mainType: Schema Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The schema defining the type used for the parameter.
        mainType: Schema Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The schema defining the type used for the parameter.
      - name: type
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The type of the parameter. Since the parameter is not located at the request body, it is limited to simple types (that is, not an object). The value MUST be one of <code>"string"</code>, <code>"number"</code>, <code>"integer"</code>, <code>"boolean"</code>, <code>"array"</code> or <code>"file"</code>. If <code>type</code> is <code>"file"</code>, the <a href="#operationConsumes"><code>consumes</code></a> MUST be either <code>"multipart/form-data"</code>, <code>" application/x-www-form-urlencoded"</code> or both and the parameter MUST be <a href="#parameterIn"><code>in</code></a> <code>"formData"</code>.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The type of the parameter. Since the parameter is not located at the request body, it is limited to simple types (that is, not an object). The value MUST be one of <code>"string"</code>, <code>"number"</code>, <code>"integer"</code>, <code>"boolean"</code>, <code>"array"</code> or <code>"file"</code>. If <code>type</code> is <code>"file"</code>, the <a href="#operationConsumes"><code>consumes</code></a> MUST be either <code>"multipart/form-data"</code>, <code>" application/x-www-form-urlencoded"</code> or both and the parameter MUST be <a href="#parameterIn"><code>in</code></a> <code>"formData"</code>.
      - name: format
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The extending format for the previously mentioned <a href="#parameterType"><code>type</code></a>. See <a href="#dataTypeFormat">Data Type Formats</a> for further details.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The extending format for the previously mentioned <a href="#parameterType"><code>type</code></a>. See <a href="#dataTypeFormat">Data Type Formats</a> for further details.
      - name: allowEmptyValue
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Sets the ability to pass empty-valued parameters. This is valid only for either <code>query</code> or <code>formData</code> parameters and allows you to send a parameter with a name only or  an empty value. Default value is <code>false</code>.
          - version: '3.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Sets the ability to pass empty-valued parameters. This is valid only for <code>query</code> parameters and allows sending a parameter with an empty value. Default value is <code>false</code>. If <a href="#parameterStyle"><code>style</code></a> is used, and if behavior is <code>n/a</code> (cannot be serialized), the value of <code>allowEmptyValue</code> SHALL be ignored. Use of this property is NOT RECOMMENDED, as it is likely to be removed in a later revision.
          - version: '3.1'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Sets the ability to pass empty-valued parameters. This is valid only for <code>query</code> parameters and allows sending a parameter with an empty value. Default value is <code>false</code>. If <a href="#parameterStyle"><code>style</code></a> is used, and if behavior is <code>n/a</code> (cannot be serialized), the value of <code>allowEmptyValue</code> SHALL be ignored. Use of this property is NOT RECOMMENDED, as it is likely to be removed in a later revision.
        mainType: boolean
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Sets the ability to pass empty-valued parameters. This is valid only for <code>query</code> parameters and allows sending a parameter with an empty value. Default value is <code>false</code>. If <a href="#parameterStyle"><code>style</code></a> is used, and if behavior is <code>n/a</code> (cannot be serialized), the value of <code>allowEmptyValue</code> SHALL be ignored. Use of this property is NOT RECOMMENDED, as it is likely to be removed in a later revision.
      - name: items
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Items Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: <strong>Required if <a href="#parameterType"><code>type</code></a> is "array".</strong> Describes the type of items in the array.
        mainType: Items Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: <strong>Required if <a href="#parameterType"><code>type</code></a> is "array".</strong> Describes the type of items in the array.
      - name: collectionFormat
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: 'Determines the format of the array if type array is used. Possible values are: <ul><li><code>csv</code> - comma separated values <code>foo,bar</code>. <li><code>ssv</code> - space separated values <code>foo bar</code>. <li><code>tsv</code> - tab separated values <code>foo\tbar</code>. <li><code>pipes</code> - pipe separated values <code>foo|bar</code>. <li><code>multi</code> - corresponds to multiple parameter instances instead of multiple values for a single instance <code>foo=bar&amp;foo=baz</code>. This is valid only for parameters <a href="#parameterIn"><code>in</code></a> "query" or "formData". </li></li></li></li></li></ul> Default value is <code>csv</code>.'
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: 'Determines the format of the array if type array is used. Possible values are: <ul><li><code>csv</code> - comma separated values <code>foo,bar</code>. <li><code>ssv</code> - space separated values <code>foo bar</code>. <li><code>tsv</code> - tab separated values <code>foo\tbar</code>. <li><code>pipes</code> - pipe separated values <code>foo|bar</code>. <li><code>multi</code> - corresponds to multiple parameter instances instead of multiple values for a single instance <code>foo=bar&amp;foo=baz</code>. This is valid only for parameters <a href="#parameterIn"><code>in</code></a> "query" or "formData". </li></li></li></li></li></ul> Default value is <code>csv</code>.'
      - name: default
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: 'Declares the value of the parameter that the server will use if none is provided, for example a "count" to control the number of results per page might default to 100 if not supplied by the client in the request. (Note: "default" has no meaning for required parameters.)  See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-6.2. Unlike JSON Schema this value MUST conform to the defined <a href="#parameterType"><code>type</code></a> for this parameter.'
        mainType: Any
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: 'Declares the value of the parameter that the server will use if none is provided, for example a "count" to control the number of results per page might default to 100 if not supplied by the client in the request. (Note: "default" has no meaning for required parameters.)  See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-6.2. Unlike JSON Schema this value MUST conform to the defined <a href="#parameterType"><code>type</code></a> for this parameter.'
      - name: maximum
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: number
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.2.
        mainType: number
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.2.
      - name: exclusiveMaximum
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.2.
        mainType: boolean
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.2.
      - name: minimum
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: number
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.3.
        mainType: number
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.3.
      - name: exclusiveMinimum
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.3.
        mainType: boolean
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.3.
      - name: maxLength
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: integer
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.2.1.
        mainType: integer
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.2.1.
      - name: minLength
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: integer
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.2.2.
        mainType: integer
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.2.2.
      - name: pattern
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.2.3.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.2.3.
      - name: maxItems
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: integer
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.3.2.
        mainType: integer
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.3.2.
      - name: minItems
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: integer
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.3.3.
        mainType: integer
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.3.3.
      - name: uniqueItems
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.3.4.
        mainType: boolean
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.3.4.
      - name: enum
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.5.1.
        mainType: Any
        parentType: array
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.5.1.
      - name: multipleOf
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: number
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.1.
        mainType: number
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.1.
      - name: ^x-
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the Swagger Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. See <a href="#vendorExtensions">Vendor Extensions</a> for further details.
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
      - name: deprecated
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Specifies that a parameter is deprecated and SHOULD be transitioned out of usage. Default value is <code>false</code>.
          - version: '3.1'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Specifies that a parameter is deprecated and SHOULD be transitioned out of usage. Default value is <code>false</code>.
        mainType: boolean
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Specifies that a parameter is deprecated and SHOULD be transitioned out of usage. Default value is <code>false</code>.
      - name: style
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: 'Describes how the parameter value will be serialized depending on the type of the parameter value. Default values (based on value of <code>in</code>): for <code>query</code> - <code>form</code>; for <code>path</code> - <code>simple</code>; for <code>header</code> - <code>simple</code>; for <code>cookie</code> - <code>form</code>.'
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: 'Describes how the parameter value will be serialized depending on the type of the parameter value. Default values (based on value of <code>in</code>): for <code>query</code> - <code>form</code>; for <code>path</code> - <code>simple</code>; for <code>header</code> - <code>simple</code>; for <code>cookie</code> - <code>form</code>.'
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: 'Describes how the parameter value will be serialized depending on the type of the parameter value. Default values (based on value of <code>in</code>): for <code>query</code> - <code>form</code>; for <code>path</code> - <code>simple</code>; for <code>header</code> - <code>simple</code>; for <code>cookie</code> - <code>form</code>.'
      - name: explode
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: When this is true, parameter values of type <code>array</code> or <code>object</code> generate separate parameters for each value of the array or key-value pair of the map. For other types of parameters this property has no effect. When <a href="#parameterStyle"><code>style</code></a> is <code>form</code>, the default value is <code>true</code>. For all other styles, the default value is <code>false</code>.
          - version: '3.1'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: When this is true, parameter values of type <code>array</code> or <code>object</code> generate separate parameters for each value of the array or key-value pair of the map. For other types of parameters this property has no effect. When <a href="#parameterStyle"><code>style</code></a> is <code>form</code>, the default value is <code>true</code>. For all other styles, the default value is <code>false</code>.
        mainType: boolean
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: When this is true, parameter values of type <code>array</code> or <code>object</code> generate separate parameters for each value of the array or key-value pair of the map. For other types of parameters this property has no effect. When <a href="#parameterStyle"><code>style</code></a> is <code>form</code>, the default value is <code>true</code>. For all other styles, the default value is <code>false</code>.
      - name: allowReserved
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Determines whether the parameter value SHOULD allow reserved characters, as defined by <a href="https://tools.ietf.org/html/rfc3986#section-2.2">RFC3986</a> <code>:/?#[]@!$&amp;'()*+,;=</code> to be included without percent-encoding. This property only applies to parameters with an <code>in</code> value of <code>query</code>. The default value is <code>false</code>.
          - version: '3.1'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Determines whether the parameter value SHOULD allow reserved characters, as defined by <a href="https://tools.ietf.org/html/rfc3986#section-2.2">RFC3986</a> <code>:/?#[]@!$&amp;'()*+,;=</code> to be included without percent-encoding. This property only applies to parameters with an <code>in</code> value of <code>query</code>. The default value is <code>false</code>.
        mainType: boolean
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Determines whether the parameter value SHOULD allow reserved characters, as defined by <a href="https://tools.ietf.org/html/rfc3986#section-2.2">RFC3986</a> <code>:/?#[]@!$&amp;'()*+,;=</code> to be included without percent-encoding. This property only applies to parameters with an <code>in</code> value of <code>query</code>. The default value is <code>false</code>.
      - name: example
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Example of the parameter's potential value. The example SHOULD match the specified schema and encoding properties if present. The <code>example</code> field is mutually exclusive of the <code>examples</code> field. Furthermore, if referencing a <code>schema</code> that contains an example, the <code>example</code> value SHALL <em>override</em> the example provided by the schema. To represent examples of media types that cannot naturally be represented in JSON or YAML, a string value can contain the example with escaping where necessary.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Example of the parameter's potential value. The example SHOULD match the specified schema and encoding properties if present. The <code>example</code> field is mutually exclusive of the <code>examples</code> field. Furthermore, if referencing a <code>schema</code> that contains an example, the <code>example</code> value SHALL <em>override</em> the example provided by the schema. To represent examples of media types that cannot naturally be represented in JSON or YAML, a string value can contain the example with escaping where necessary.
        mainType: Any
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Example of the parameter's potential value. The example SHOULD match the specified schema and encoding properties if present. The <code>example</code> field is mutually exclusive of the <code>examples</code> field. Furthermore, if referencing a <code>schema</code> that contains an example, the <code>example</code> value SHALL <em>override</em> the example provided by the schema. To represent examples of media types that cannot naturally be represented in JSON or YAML, a string value can contain the example with escaping where necessary.
      - name: examples
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Example Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: Examples of the parameter's potential value. Each example SHOULD contain a value in the correct format as specified in the parameter encoding. The <code>examples</code> field is mutually exclusive of the <code>example</code> field. Furthermore, if referencing a <code>schema</code> that contains an example, the <code>examples</code> value SHALL <em>override</em> the example provided by the schema.
          - version: '3.1'
            mainType: Example Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: Examples of the parameter's potential value. Each example SHOULD contain a value in the correct format as specified in the parameter encoding. The <code>examples</code> field is mutually exclusive of the <code>example</code> field. Furthermore, if referencing a <code>schema</code> that contains an example, the <code>examples</code> value SHALL <em>override</em> the example provided by the schema.
        mainType: Example Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: true
        description: Examples of the parameter's potential value. Each example SHOULD contain a value in the correct format as specified in the parameter encoding. The <code>examples</code> field is mutually exclusive of the <code>example</code> field. Furthermore, if referencing a <code>schema</code> that contains an example, the <code>examples</code> value SHALL <em>override</em> the example provided by the schema.
      - name: content
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Media Type Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A map containing the representations for the parameter. The key is the media type and the value describes it. The map MUST only contain one entry.
          - version: '3.1'
            mainType: Media Type Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A map containing the representations for the parameter. The key is the media type and the value describes it. The map MUST only contain one entry.
        mainType: Media Type Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A map containing the representations for the parameter. The key is the media type and the value describes it. The map MUST only contain one entry.
  - name: Items Object
    isRoot: false
    versions:
      - '2.0'
    logs:
      - version: '2.0'
        isExtensible: true
        description: <p>A limited subset of JSON-Schema's items object. It is used by parameter definitions that are not located <a href="#parameterIn"><code>in</code></a> <code>"body"</code>.</p>
    isExtensible: true
    description: <p>A limited subset of JSON-Schema's items object. It is used by parameter definitions that are not located <a href="#parameterIn"><code>in</code></a> <code>"body"</code>.</p>
    fullName: Items Object (2.0)
    fields:
      - name: type
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The internal type of the array. The value MUST be one of <code>"string"</code>, <code>"number"</code>, <code>"integer"</code>, <code>"boolean"</code>, or <code>"array"</code>. Files and models are not allowed.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The internal type of the array. The value MUST be one of <code>"string"</code>, <code>"number"</code>, <code>"integer"</code>, <code>"boolean"</code>, or <code>"array"</code>. Files and models are not allowed.
      - name: format
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The extending format for the previously mentioned <a href="#parameterType"><code>type</code></a>. See <a href="#dataTypeFormat">Data Type Formats</a> for further details.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The extending format for the previously mentioned <a href="#parameterType"><code>type</code></a>. See <a href="#dataTypeFormat">Data Type Formats</a> for further details.
      - name: items
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Items Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: <strong>Required if <a href="#itemsType"><code>type</code></a> is "array".</strong> Describes the type of items in the array.
        mainType: Items Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: <strong>Required if <a href="#itemsType"><code>type</code></a> is "array".</strong> Describes the type of items in the array.
      - name: collectionFormat
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: 'Determines the format of the array if type array is used. Possible values are: <ul><li><code>csv</code> - comma separated values <code>foo,bar</code>. <li><code>ssv</code> - space separated values <code>foo bar</code>. <li><code>tsv</code> - tab separated values <code>foo\tbar</code>. <li><code>pipes</code> - pipe separated values <code>foo|bar</code>. </li></li></li></li></ul> Default value is <code>csv</code>.'
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: 'Determines the format of the array if type array is used. Possible values are: <ul><li><code>csv</code> - comma separated values <code>foo,bar</code>. <li><code>ssv</code> - space separated values <code>foo bar</code>. <li><code>tsv</code> - tab separated values <code>foo\tbar</code>. <li><code>pipes</code> - pipe separated values <code>foo|bar</code>. </li></li></li></li></ul> Default value is <code>csv</code>.'
      - name: default
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: 'Declares the value of the item that the server will use if none is provided. (Note: "default" has no meaning for required items.) See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-6.2. Unlike JSON Schema this value MUST conform to the defined <a href="#itemsType"><code>type</code></a> for the data type.'
        mainType: Any
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: 'Declares the value of the item that the server will use if none is provided. (Note: "default" has no meaning for required items.) See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-6.2. Unlike JSON Schema this value MUST conform to the defined <a href="#itemsType"><code>type</code></a> for the data type.'
      - name: maximum
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: number
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.2.
        mainType: number
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.2.
      - name: exclusiveMaximum
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.2.
        mainType: boolean
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.2.
      - name: minimum
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: number
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.3.
        mainType: number
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.3.
      - name: exclusiveMinimum
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.3.
        mainType: boolean
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.3.
      - name: maxLength
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: integer
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.2.1.
        mainType: integer
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.2.1.
      - name: minLength
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: integer
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.2.2.
        mainType: integer
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.2.2.
      - name: pattern
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.2.3.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.2.3.
      - name: maxItems
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: integer
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.3.2.
        mainType: integer
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.3.2.
      - name: minItems
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: integer
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.3.3.
        mainType: integer
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.3.3.
      - name: uniqueItems
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.3.4.
        mainType: boolean
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.3.4.
      - name: enum
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.5.1.
        mainType: Any
        parentType: array
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.5.1.
      - name: multipleOf
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: number
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.1.
        mainType: number
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.1.
      - name: ^x-
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the Swagger Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. See <a href="#vendorExtensions">Vendor Extensions</a> for further details.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the Swagger Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. See <a href="#vendorExtensions">Vendor Extensions</a> for further details.
  - name: Responses Object
    isRoot: false
    versions:
      - '2.0'
      - '3.0'
      - '3.1'
    logs:
      - version: '2.0'
        isExtensible: true
        description: <p>A container for the expected responses of an operation. The container maps a HTTP response code to the expected response. It is not expected from the documentation to necessarily cover all possible HTTP response codes, since they may not be known in advance. However, it is expected from the documentation to cover a successful operation response and any known errors.</p><p>The <code>default</code> can be used as the default response object for all HTTP codes that are not covered individually by the specification.</p><p>The <code>Responses Object</code> MUST contain at least one response code, and it SHOULD be the response for a successful operation call.</p>
      - version: '3.0'
        isExtensible: true
        description: |-
          <p>A container for the expected responses of an operation.
          The container maps a HTTP response code to the expected response.</p><p>The documentation is not necessarily expected to cover all possible HTTP response codes because they may not be known in advance.
          However, documentation is expected to cover a successful operation response and any known errors.</p><p>The <code>default</code> MAY be used as a default response object for all HTTP codes 
          that are not covered individually by the specification.</p><p>The <code>Responses Object</code> MUST contain at least one response code, and it 
          SHOULD be the response for a successful operation call.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: |-
          <p>A container for the expected responses of an operation.
          The container maps a HTTP response code to the expected response.</p><p>The documentation is not necessarily expected to cover all possible HTTP response codes because they may not be known in advance.
          However, documentation is expected to cover a successful operation response and any known errors.</p><p>The <code>default</code> MAY be used as a default response object for all HTTP codes 
          that are not covered individually by the <code>Responses Object</code>.</p><p>The <code>Responses Object</code> MUST contain at least one response code, and if only one
          response code is provided it SHOULD be the response for a successful operation
          call.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: |-
      <p>A container for the expected responses of an operation.
      The container maps a HTTP response code to the expected response.</p><p>The documentation is not necessarily expected to cover all possible HTTP response codes because they may not be known in advance.
      However, documentation is expected to cover a successful operation response and any known errors.</p><p>The <code>default</code> MAY be used as a default response object for all HTTP codes 
      that are not covered individually by the <code>Responses Object</code>.</p><p>The <code>Responses Object</code> MUST contain at least one response code, and if only one
      response code is provided it SHOULD be the response for a successful operation
      call.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Responses Object (2.0, 3.0, 3.1)
    fields:
      - name: default
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Response Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: The documentation of responses other than the ones declared for specific HTTP response codes. It can be used to cover undeclared responses. <a href="#referenceObject">Reference Object</a> can be used to link to a response that is defined at the <a href="#swaggerResponses">Swagger Object's responses</a> section.
          - version: '3.0'
            mainType: Response Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: The documentation of responses other than the ones declared for specific HTTP response codes. Use this field to cover undeclared responses. A <a href="#referenceObject">Reference Object</a> can link to a response that the <a href="#componentsResponses">OpenAPI Object's components/responses</a> section defines.
          - version: '3.1'
            mainType: Response Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: The documentation of responses other than the ones declared for specific HTTP response codes. Use this field to cover undeclared responses.
        mainType: Response Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: true
        description: The documentation of responses other than the ones declared for specific HTTP response codes. Use this field to cover undeclared responses.
      - name: '{HTTP Status Code}'
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Response Object
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: true
            description: Any <a href="#httpCodes">HTTP status code</a> can be used as the property name (one property per HTTP status code). Describes the expected response for that HTTP status code.  <a href="#referenceObject">Reference Object</a> can be used to link to a response that is defined at the <a href="#swaggerResponses">Swagger Object's responses</a> section.
        mainType: Response Object
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: true
        description: Any <a href="#httpCodes">HTTP status code</a> can be used as the property name (one property per HTTP status code). Describes the expected response for that HTTP status code.  <a href="#referenceObject">Reference Object</a> can be used to link to a response that is defined at the <a href="#swaggerResponses">Swagger Object's responses</a> section.
      - name: ^x-
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the Swagger Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. See <a href="#vendorExtensions">Vendor Extensions</a> for further details.
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
      - name: HTTP Status Code
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Response Object
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: true
            description: 'Any <a href="#httpCodes">HTTP status code</a> can be used as the property name, but only one property per code, to describe the expected response for that HTTP status code.  A <a href="#referenceObject">Reference Object</a> can link to a response that is defined in the <a href="#componentsResponses">OpenAPI Object''s components/responses</a> section. This field MUST be enclosed in quotation marks (for example, "200") for compatibility between JSON and YAML. To define a range of response codes, this field MAY contain the uppercase wildcard character <code>X</code>. For example, <code>2XX</code> represents all response codes between <code>[200-299]</code>. Only the following range definitions are allowed: <code>1XX</code>, <code>2XX</code>, <code>3XX</code>, <code>4XX</code>, and <code>5XX</code>. If a response is defined using an explicit code, the explicit code definition takes precedence over the range definition for that code.'
          - version: '3.1'
            mainType: Response Object
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: true
            description: 'Any <a href="#httpCodes">HTTP status code</a> can be used as the property name, but only one property per code, to describe the expected response for that HTTP status code. This field MUST be enclosed in quotation marks (for example, "200") for compatibility between JSON and YAML. To define a range of response codes, this field MAY contain the uppercase wildcard character <code>X</code>. For example, <code>2XX</code> represents all response codes between <code>[200-299]</code>. Only the following range definitions are allowed: <code>1XX</code>, <code>2XX</code>, <code>3XX</code>, <code>4XX</code>, and <code>5XX</code>. If a response is defined using an explicit code, the explicit code definition takes precedence over the range definition for that code.'
        mainType: Response Object
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: true
        description: 'Any <a href="#httpCodes">HTTP status code</a> can be used as the property name, but only one property per code, to describe the expected response for that HTTP status code. This field MUST be enclosed in quotation marks (for example, "200") for compatibility between JSON and YAML. To define a range of response codes, this field MAY contain the uppercase wildcard character <code>X</code>. For example, <code>2XX</code> represents all response codes between <code>[200-299]</code>. Only the following range definitions are allowed: <code>1XX</code>, <code>2XX</code>, <code>3XX</code>, <code>4XX</code>, and <code>5XX</code>. If a response is defined using an explicit code, the explicit code definition takes precedence over the range definition for that code.'
  - name: Response Object
    isRoot: false
    versions:
      - '2.0'
      - '3.0'
      - '3.1'
    logs:
      - version: '2.0'
        isExtensible: true
        description: <p>Describes a single response from an API Operation.</p>
      - version: '3.0'
        isExtensible: true
        description: |-
          <p>Describes a single response from an API Operation, including design-time, static 
          <code>links</code> to operations based on the response.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: |-
          <p>Describes a single response from an API Operation, including design-time, static 
          <code>links</code> to operations based on the response.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: |-
      <p>Describes a single response from an API Operation, including design-time, static 
      <code>links</code> to operations based on the response.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Response Object (2.0, 3.0, 3.1)
    fields:
      - name: description
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A short description of the response.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: A short description of the response.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: A description of the response.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: A description of the response.
      - name: schema
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Schema Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A definition of the response structure. It can be a primitive, an array or an object. If this field does not exist, it means no content is returned as part of the response. As an extension to the <a href="#schemaObject">Schema Object</a>, its root <code>type</code> value may also be <code>"file"</code>. This SHOULD be accompanied by a relevant <code>produces</code> mime-type.
        mainType: Schema Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A definition of the response structure. It can be a primitive, an array or an object. If this field does not exist, it means no content is returned as part of the response. As an extension to the <a href="#schemaObject">Schema Object</a>, its root <code>type</code> value may also be <code>"file"</code>. This SHOULD be accompanied by a relevant <code>produces</code> mime-type.
      - name: headers
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Headers Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A list of headers that are sent with the response.
          - version: '3.0'
            mainType: Header Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: Maps a header name to its definition. <a href="https://tools.ietf.org/html/rfc7230#page-22">RFC7230</a> states header names are case insensitive. If a response header is defined with the name <code>"Content-Type"</code>, it SHALL be ignored.
          - version: '3.1'
            mainType: Header Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: Maps a header name to its definition. <a href="https://tools.ietf.org/html/rfc7230#page-22">RFC7230</a> states header names are case insensitive. If a response header is defined with the name <code>"Content-Type"</code>, it SHALL be ignored.
        mainType: Header Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: true
        description: Maps a header name to its definition. <a href="https://tools.ietf.org/html/rfc7230#page-22">RFC7230</a> states header names are case insensitive. If a response header is defined with the name <code>"Content-Type"</code>, it SHALL be ignored.
      - name: examples
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Example Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An example of the response message.
        mainType: Example Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: An example of the response message.
      - name: ^x-
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the Swagger Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. See <a href="#vendorExtensions">Vendor Extensions</a> for further details.
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
      - name: content
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Media Type Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A map containing descriptions of potential response payloads. The key is a media type or <a href="https://tools.ietf.org/html/rfc7231#appendix-D">media type range</a> and the value describes it.  For responses that match multiple keys, only the most specific key is applicable. e.g. text/plain overrides text/*
          - version: '3.1'
            mainType: Media Type Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A map containing descriptions of potential response payloads. The key is a media type or <a href="https://tools.ietf.org/html/rfc7231#appendix-D">media type range</a> and the value describes it.  For responses that match multiple keys, only the most specific key is applicable. e.g. text/plain overrides text/*
        mainType: Media Type Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A map containing descriptions of potential response payloads. The key is a media type or <a href="https://tools.ietf.org/html/rfc7231#appendix-D">media type range</a> and the value describes it.  For responses that match multiple keys, only the most specific key is applicable. e.g. text/plain overrides text/*
      - name: links
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Link Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: A map of operations links that can be followed from the response. The key of the map is a short name for the link, following the naming constraints of the names for <a href="#componentsObject">Component Objects</a>.
          - version: '3.1'
            mainType: Link Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: A map of operations links that can be followed from the response. The key of the map is a short name for the link, following the naming constraints of the names for <a href="#componentsObject">Component Objects</a>.
        mainType: Link Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: true
        description: A map of operations links that can be followed from the response. The key of the map is a short name for the link, following the naming constraints of the names for <a href="#componentsObject">Component Objects</a>.
  - name: Headers Object
    isRoot: false
    versions:
      - '2.0'
    logs:
      - version: '2.0'
        isExtensible: false
        description: <p>Lists the headers that can be sent as part of a response.</p>
    isExtensible: false
    description: <p>Lists the headers that can be sent as part of a response.</p>
    fullName: Headers Object (2.0)
    fields:
      - name: '{name}'
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Header Object
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: The name of the property corresponds to the name of the header. The value describes the type of the header.
        mainType: Header Object
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: The name of the property corresponds to the name of the header. The value describes the type of the header.
  - name: Example Object
    isRoot: false
    versions:
      - '2.0'
      - '3.0'
      - '3.1'
    logs:
      - version: '2.0'
        isExtensible: false
        description: <p>Allows sharing examples for operation responses.</p>
      - version: '3.0'
        isExtensible: true
        description: <p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: <p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: <p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Example Object (2.0, 3.0, 3.1)
    fields:
      - name: '{mime type}'
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: The name of the property MUST be one of the Operation <code>produces</code> values (either implicit or inherited). The value SHOULD be an example of what such a response would look like.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: The name of the property MUST be one of the Operation <code>produces</code> values (either implicit or inherited). The value SHOULD be an example of what such a response would look like.
      - name: summary
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Short description for the example.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Short description for the example.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Short description for the example.
      - name: description
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Long description for the example.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Long description for the example.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Long description for the example.
      - name: value
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Embedded literal example. The <code>value</code> field and <code>externalValue</code> field are mutually exclusive. To represent examples of media types that cannot naturally represented in JSON or YAML, use a string value to contain the example, escaping where necessary.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Embedded literal example. The <code>value</code> field and <code>externalValue</code> field are mutually exclusive. To represent examples of media types that cannot naturally represented in JSON or YAML, use a string value to contain the example, escaping where necessary.
        mainType: Any
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Embedded literal example. The <code>value</code> field and <code>externalValue</code> field are mutually exclusive. To represent examples of media types that cannot naturally represented in JSON or YAML, use a string value to contain the example, escaping where necessary.
      - name: externalValue
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A URL that points to the literal example. This provides the capability to reference examples that cannot easily be included in JSON or YAML documents.  The <code>value</code> field and <code>externalValue</code> field are mutually exclusive.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A URI that points to the literal example. This provides the capability to reference examples that cannot easily be included in JSON or YAML documents.  The <code>value</code> field and <code>externalValue</code> field are mutually exclusive. See the rules for resolving <a href="#relativeReferencesURI">Relative References</a>.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A URI that points to the literal example. This provides the capability to reference examples that cannot easily be included in JSON or YAML documents.  The <code>value</code> field and <code>externalValue</code> field are mutually exclusive. See the rules for resolving <a href="#relativeReferencesURI">Relative References</a>.
      - name: ^x-
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
  - name: Header Object
    isRoot: false
    versions:
      - '2.0'
      - '3.0'
      - '3.1'
    logs:
      - version: '2.0'
        isExtensible: true
        description: |-
          <table>
          <thead>
          <tr>
          <th>Field Name</th>
          <th style="text-align: center;">Type</th>
          <th>Description</th>
          </tr>
          </thead>
          <tbody>
          <tr>
          <td><a name="headerDescription"></a>description</td>
          <td style="text-align: center;"><code>string</code></td>
          <td>A short description of the header.</td>
          </tr>
          <tr>
          <td><a name="headerType"></a>type</td>
          <td style="text-align: center;"><code>string</code></td>
          <td><strong>Required.</strong> The type of the object. The value MUST be one of <code>"string"</code>, <code>"number"</code>, <code>"integer"</code>, <code>"boolean"</code>, or <code>"array"</code>.</td>
          </tr>
          <tr>
          <td><a name="headerFormat"></a>format</td>
          <td style="text-align: center;"><code>string</code></td>
          <td>The extending format for the previously mentioned <a href="#stType"><code>type</code></a>. See <a href="#dataTypeFormat">Data Type Formats</a> for further details.</td>
          </tr>
          <tr>
          <td><a name="headerItems"></a>items</td>
          <td style="text-align: center;"><a href="#itemsObject">Items Object</a></td>
          <td><strong>Required if <a href="#stType"><code>type</code></a> is "array".</strong> Describes the type of items in the array.</td>
          </tr>
          <tr>
          <td><a name="headerCollectionFormat"></a>collectionFormat</td>
          <td style="text-align: center;"><code>string</code></td>
          <td>Determines the format of the array if type array is used. Possible values are: <ul><li><code>csv</code> - comma separated values <code>foo,bar</code>. <li><code>ssv</code> - space separated values <code>foo bar</code>. <li><code>tsv</code> - tab separated values <code>foo\tbar</code>. <li><code>pipes</code> - pipe separated values <code>foo|bar</code>. </li></li></li></li></ul> Default value is <code>csv</code>.</td>
          </tr>
          <tr>
          <td><a name="headerDefault"></a>default</td>
          <td style="text-align: center;">*</td>
          <td>Declares the value of the header that the server will use if none is provided. (Note: "default" has no meaning for required headers.) See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-6.2. Unlike JSON Schema this value MUST conform to the defined <a href="#headerDefault"><code>type</code></a> for the header.</td>
          </tr>
          <tr>
          <td><a name="headerMaximum"></a>maximum</td>
          <td style="text-align: center;"><code>number</code></td>
          <td>See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.2.</td>
          </tr>
          <tr>
          <td><a name="headerMaximum"></a>exclusiveMaximum</td>
          <td style="text-align: center;"><code>boolean</code></td>
          <td>See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.2.</td>
          </tr>
          <tr>
          <td><a name="headerMinimum"></a>minimum</td>
          <td style="text-align: center;"><code>number</code></td>
          <td>See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.3.</td>
          </tr>
          <tr>
          <td><a name="headerExclusiveMinimum"></a>exclusiveMinimum</td>
          <td style="text-align: center;"><code>boolean</code></td>
          <td>See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.3.</td>
          </tr>
          <tr>
          <td><a name="headerMaxLength"></a>maxLength</td>
          <td style="text-align: center;"><code>integer</code></td>
          <td>See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.2.1.</td>
          </tr>
          <tr>
          <td><a name="headerMinLength"></a>minLength</td>
          <td style="text-align: center;"><code>integer</code></td>
          <td>See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.2.2.</td>
          </tr>
          <tr>
          <td><a name="headerPattern"></a>pattern</td>
          <td style="text-align: center;"><code>string</code></td>
          <td>See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.2.3.</td>
          </tr>
          <tr>
          <td><a name="headerMaxItems"></a>maxItems</td>
          <td style="text-align: center;"><code>integer</code></td>
          <td>See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.3.2.</td>
          </tr>
          <tr>
          <td><a name="headerMinItems"></a>minItems</td>
          <td style="text-align: center;"><code>integer</code></td>
          <td>See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.3.3.</td>
          </tr>
          <tr>
          <td><a name="headerUniqueItems"></a>uniqueItems</td>
          <td style="text-align: center;"><code>boolean</code></td>
          <td>https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.3.4.</td>
          </tr>
          <tr>
          <td><a name="headerEnum"></a>enum</td>
          <td style="text-align: center;">[*]</td>
          <td>See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.5.1.</td>
          </tr>
          <tr>
          <td><a name="headerMultipleOf"></a>multipleOf</td>
          <td style="text-align: center;"><code>number</code></td>
          <td>See https://tools.ietf.org/html/draft-fge-json-schema-validation-00#section-5.1.1.</td>
          </tr>
          </tbody>
          </table>
      - version: '3.0'
        isExtensible: false
        description: |-
          <p>The Header Object follows the structure of the <a href="#parameterObject">Parameter Object</a> with the following changes:</p><ol>
          <li><code>name</code> MUST NOT be specified, it is given in the corresponding <code>headers</code> map.</li>
          <li><code>in</code> MUST NOT be specified, it is implicitly in <code>header</code>.</li>
          <li>All traits that are affected by the location MUST be applicable to a location of <code>header</code> (for example, <a href="#parameterStyle"><code>style</code></a>).</li>
          </ol><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: false
        description: |-
          <p>The Header Object follows the structure of the <a href="#parameterObject">Parameter Object</a> with the following changes:</p><ol>
          <li><code>name</code> MUST NOT be specified, it is given in the corresponding <code>headers</code> map.</li>
          <li><code>in</code> MUST NOT be specified, it is implicitly in <code>header</code>.</li>
          <li>All traits that are affected by the location MUST be applicable to a location of <code>header</code> (for example, <a href="#parameterStyle"><code>style</code></a>).</li>
          </ol><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: false
    description: |-
      <p>The Header Object follows the structure of the <a href="#parameterObject">Parameter Object</a> with the following changes:</p><ol>
      <li><code>name</code> MUST NOT be specified, it is given in the corresponding <code>headers</code> map.</li>
      <li><code>in</code> MUST NOT be specified, it is implicitly in <code>header</code>.</li>
      <li>All traits that are affected by the location MUST be applicable to a location of <code>header</code> (for example, <a href="#parameterStyle"><code>style</code></a>).</li>
      </ol><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Header Object (2.0, 3.0, 3.1)
    fields:
      - name: ^x-
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the Swagger Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. See <a href="#vendorExtensions">Vendor Extensions</a> for further details.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the Swagger Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. See <a href="#vendorExtensions">Vendor Extensions</a> for further details.
  - name: Tag Object
    isRoot: false
    versions:
      - '2.0'
      - '3.0'
      - '3.1'
    logs:
      - version: '2.0'
        isExtensible: true
        description: <p>Allows adding meta data to a single tag that is used by the <a href="#operationObject">Operation Object</a>. It is not mandatory to have a Tag Object per tag used there.</p>
      - version: '3.0'
        isExtensible: true
        description: |-
          <p>Adds metadata to a single tag that is used by the <a href="#operationObject">Operation Object</a>.
          It is not mandatory to have a Tag Object per tag defined in the Operation Object instances.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: |-
          <p>Adds metadata to a single tag that is used by the <a href="#operationObject">Operation Object</a>.
          It is not mandatory to have a Tag Object per tag defined in the Operation Object instances.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: |-
      <p>Adds metadata to a single tag that is used by the <a href="#operationObject">Operation Object</a>.
      It is not mandatory to have a Tag Object per tag defined in the Operation Object instances.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Tag Object (2.0, 3.0, 3.1)
    fields:
      - name: name
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The name of the tag.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The name of the tag.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The name of the tag.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: The name of the tag.
      - name: description
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A short description for the tag.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A short description for the tag.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A description for the tag.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A description for the tag.
      - name: externalDocs
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: External Documentation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Additional external documentation for this tag.
          - version: '3.0'
            mainType: External Documentation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Additional external documentation for this tag.
          - version: '3.1'
            mainType: External Documentation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Additional external documentation for this tag.
        mainType: External Documentation Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Additional external documentation for this tag.
      - name: ^x-
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the Swagger Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. See <a href="#vendorExtensions">Vendor Extensions</a> for further details.
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
  - name: Reference Object
    isRoot: false
    versions:
      - '2.0'
      - '3.0'
      - '3.1'
    logs:
      - version: '2.0'
        isExtensible: false
        description: <p>A simple object to allow referencing other definitions in the specification. It can be used to reference parameters and responses that are defined at the top level for reuse.</p><p>The Reference Object is a <a href="http://tools.ietf.org/html/draft-pbryan-zyp-json-ref-02">JSON Reference</a> that uses a <a href="http://tools.ietf.org/html/rfc6901">JSON Pointer</a> as its value. For this specification, only <a href="https://tools.ietf.org/html/draft-zyp-json-schema-04#section-7.2.3">canonical dereferencing</a> is supported.</p>
      - version: '3.0'
        isExtensible: false
        description: <p>A simple object to allow referencing other components in the specification, internally and externally.</p><p>The Reference Object is defined by <a href="https://tools.ietf.org/html/draft-pbryan-zyp-json-ref-03">JSON Reference</a> and follows the same structure, behavior and rules. </p><p>For this specification, reference resolution is accomplished as defined by the JSON Reference specification and not by the JSON Schema specification.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: false
        description: <p>A simple object to allow referencing other components in the OpenAPI document, internally and externally.</p><p>The <code>$ref</code> string value contains a URI <a href="https://tools.ietf.org/html/rfc3986">RFC3986</a>, which identifies the location of the value being referenced.</p><p>See the rules for resolving <a href="#relativeReferencesURI">Relative References</a>.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: false
    description: <p>A simple object to allow referencing other components in the OpenAPI document, internally and externally.</p><p>The <code>$ref</code> string value contains a URI <a href="https://tools.ietf.org/html/rfc3986">RFC3986</a>, which identifies the location of the value being referenced.</p><p>See the rules for resolving <a href="#relativeReferencesURI">Relative References</a>.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Reference Object (2.0, 3.0, 3.1)
    fields:
      - name: $ref
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The reference string.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The reference string.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The reference identifier. This MUST be in the form of a URI.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: The reference identifier. This MUST be in the form of a URI.
      - name: summary
        versions:
          - '3.1'
        logs:
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A short summary which by default SHOULD override that of the referenced component. If the referenced object-type does not allow a <code>summary</code> field, then this field has no effect.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A short summary which by default SHOULD override that of the referenced component. If the referenced object-type does not allow a <code>summary</code> field, then this field has no effect.
      - name: description
        versions:
          - '3.1'
        logs:
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A description which by default SHOULD override that of the referenced component.If the referenced object-type does not allow a <code>description</code> field, then this field has no effect.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A description which by default SHOULD override that of the referenced component.If the referenced object-type does not allow a <code>description</code> field, then this field has no effect.
  - name: Schema Object
    isRoot: false
    versions:
      - '2.0'
      - '3.0'
      - '3.1'
    logs:
      - version: '2.0'
        isExtensible: true
        description: |-
          <p>The Schema Object allows the definition of input and output data types. These types can be objects, but also primitives and arrays. This object is based on the <a href="http://json-schema.org/">JSON Schema Specification Draft 4</a> and uses a predefined subset of it. On top of this subset, there are extensions provided by this specification to allow for more complete documentation.</p><p>Further information about the properties can be found in <a href="https://tools.ietf.org/html/draft-zyp-json-schema-04">JSON Schema Core</a> and <a href="https://tools.ietf.org/html/draft-fge-json-schema-validation-00">JSON Schema Validation</a>. Unless stated otherwise, the property definitions follow the JSON Schema specification as referenced here.</p><p>The following properties are taken directly from the JSON Schema definition and follow the same specifications:
          - $ref - As a <a href="https://tools.ietf.org/html/draft-pbryan-zyp-json-ref-03">JSON Reference</a>
          - format (See <a href="#dataTypeFormat">Data Type Formats</a> for further details)
          - title
          - description (<a href="https://guides.github.com/features/mastering-markdown/#GitHub-flavored-markdown">GFM syntax</a> can be used for rich text representation)
          - default (Unlike JSON Schema, the value MUST conform to the defined type for the Schema Object)
          - multipleOf
          - maximum
          - exclusiveMaximum
          - minimum
          - exclusiveMinimum
          - maxLength
          - minLength
          - pattern
          - maxItems
          - minItems
          - uniqueItems
          - maxProperties
          - minProperties
          - required
          - enum
          - type</p><p>The following properties are taken from the JSON Schema definition but their definitions were adjusted to the Swagger Specification. Their definition is the same as the one from JSON Schema, only where the original definition references the JSON Schema definition, the <a href="#schemaObject">Schema Object</a> definition is used instead.
          - items
          - allOf
          - properties
          - additionalProperties</p><p>Other than the JSON Schema subset fields, the following fields may be used for further schema documentation.</p>
      - version: '3.0'
        isExtensible: true
        description: |-
          <p>The Schema Object allows the definition of input and output data types.
          These types can be objects, but also primitives and arrays.
          This object is an extended subset of the <a href="https://json-schema.org/">JSON Schema Specification Wright Draft 00</a>.</p><p>For more information about the properties, see <a href="https://tools.ietf.org/html/draft-wright-json-schema-00">JSON Schema Core</a> and <a href="https://tools.ietf.org/html/draft-wright-json-schema-validation-00">JSON Schema Validation</a>.
          Unless stated otherwise, the property definitions follow the JSON Schema.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: |-
          <p>The Schema Object allows the definition of input and output data types.
          These types can be objects, but also primitives and arrays. This object is a superset of the <a href="https://tools.ietf.org/html/draft-bhutton-json-schema-00">JSON Schema Specification Draft 2020-12</a>.</p><p>For more information about the properties, see <a href="https://tools.ietf.org/html/draft-bhutton-json-schema-00">JSON Schema Core</a> and <a href="https://tools.ietf.org/html/draft-bhutton-json-schema-validation-00">JSON Schema Validation</a>.</p><p>Unless stated otherwise, the property definitions follow those of JSON Schema and do not add any additional semantics.
          Where JSON Schema indicates that behavior is defined by the application (e.g. for annotations), OAS also defers the definition of semantics to the application consuming the OpenAPI document.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: |-
      <p>The Schema Object allows the definition of input and output data types.
      These types can be objects, but also primitives and arrays. This object is a superset of the <a href="https://tools.ietf.org/html/draft-bhutton-json-schema-00">JSON Schema Specification Draft 2020-12</a>.</p><p>For more information about the properties, see <a href="https://tools.ietf.org/html/draft-bhutton-json-schema-00">JSON Schema Core</a> and <a href="https://tools.ietf.org/html/draft-bhutton-json-schema-validation-00">JSON Schema Validation</a>.</p><p>Unless stated otherwise, the property definitions follow those of JSON Schema and do not add any additional semantics.
      Where JSON Schema indicates that behavior is defined by the application (e.g. for annotations), OAS also defers the definition of semantics to the application consuming the OpenAPI document.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Schema Object (2.0, 3.0, 3.1)
    fields:
      - name: discriminator
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Adds support for polymorphism. The discriminator is the schema property name that is used to differentiate between other schema that inherit this schema. The property name used MUST be defined at this schema and it MUST be in the <code>required</code> property list. When used, the value MUST be the name of this schema or any schema that inherits it.
          - version: '3.0'
            mainType: Discriminator Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Adds support for polymorphism. The discriminator is an object name that is used to differentiate between other schemas which may satisfy the payload description. See <a href="#schemaComposition">Composition and Inheritance</a> for more details.
          - version: '3.1'
            mainType: Discriminator Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Adds support for polymorphism. The discriminator is an object name that is used to differentiate between other schemas which may satisfy the payload description. See <a href="#schemaComposition">Composition and Inheritance</a> for more details.
        mainType: Discriminator Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Adds support for polymorphism. The discriminator is an object name that is used to differentiate between other schemas which may satisfy the payload description. See <a href="#schemaComposition">Composition and Inheritance</a> for more details.
      - name: readOnly
        versions:
          - '2.0'
          - '3.0'
        logs:
          - version: '2.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Relevant only for Schema <code>"properties"</code> definitions. Declares the property as "read only". This means that it MAY be sent as part of a response but MUST NOT be sent as part of the request. Properties marked as <code>readOnly</code> being <code>true</code> SHOULD NOT be in the <code>required</code> list of the defined schema. Default value is <code>false</code>.
          - version: '3.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Relevant only for Schema <code>"properties"</code> definitions. Declares the property as "read only". This means that it MAY be sent as part of a response but SHOULD NOT be sent as part of the request. If the property is marked as <code>readOnly</code> being <code>true</code> and is in the <code>required</code> list, the <code>required</code> will take effect on the response only. A property MUST NOT be marked as both <code>readOnly</code> and <code>writeOnly</code> being <code>true</code>. Default value is <code>false</code>.
        mainType: boolean
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Relevant only for Schema <code>"properties"</code> definitions. Declares the property as "read only". This means that it MAY be sent as part of a response but SHOULD NOT be sent as part of the request. If the property is marked as <code>readOnly</code> being <code>true</code> and is in the <code>required</code> list, the <code>required</code> will take effect on the response only. A property MUST NOT be marked as both <code>readOnly</code> and <code>writeOnly</code> being <code>true</code>. Default value is <code>false</code>.
      - name: xml
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: XML Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: This MAY be used only on properties schemas. It has no effect on root schemas. Adds Additional metadata to describe the XML representation format of this property.
          - version: '3.0'
            mainType: XML Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: This MAY be used only on properties schemas. It has no effect on root schemas. Adds additional metadata to describe the XML representation of this property.
          - version: '3.1'
            mainType: XML Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: This MAY be used only on properties schemas. It has no effect on root schemas. Adds additional metadata to describe the XML representation of this property.
        mainType: XML Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: This MAY be used only on properties schemas. It has no effect on root schemas. Adds additional metadata to describe the XML representation of this property.
      - name: externalDocs
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: External Documentation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Additional external documentation for this schema.
          - version: '3.0'
            mainType: External Documentation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Additional external documentation for this schema.
          - version: '3.1'
            mainType: External Documentation Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Additional external documentation for this schema.
        mainType: External Documentation Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Additional external documentation for this schema.
      - name: example
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A free-form property to include an example of an instance for this schema.
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A free-form property to include an example of an instance for this schema. To represent examples that cannot be naturally represented in JSON or YAML, a string value can be used to contain the example with escaping where necessary.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A free-form property to include an example of an instance for this schema. To represent examples that cannot be naturally represented in JSON or YAML, a string value can be used to contain the example with escaping where necessary.<br/><br/><strong>Deprecated:</strong> The <code>example</code> property has been deprecated in favor of the JSON Schema <code>examples</code> keyword. Use of <code>example</code> is discouraged, and later versions of this specification may remove it.
        mainType: Any
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A free-form property to include an example of an instance for this schema. To represent examples that cannot be naturally represented in JSON or YAML, a string value can be used to contain the example with escaping where necessary.<br/><br/><strong>Deprecated:</strong> The <code>example</code> property has been deprecated in favor of the JSON Schema <code>examples</code> keyword. Use of <code>example</code> is discouraged, and later versions of this specification may remove it.
      - name: ^x-
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the Swagger Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. See <a href="#vendorExtensions">Vendor Extensions</a> for further details.
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
      - name: nullable
        versions:
          - '3.0'
        logs:
          - version: '3.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A <code>true</code> value adds <code>"null"</code> to the allowed type specified by the <code>type</code> keyword, only if <code>type</code> is explicitly defined within the same Schema Object. Other Schema Object constraints retain their defined behavior, and therefore may disallow the use of <code>null</code> as a value. A <code>false</code> value leaves the specified or default <code>type</code> unmodified. The default value is <code>false</code>.
        mainType: boolean
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A <code>true</code> value adds <code>"null"</code> to the allowed type specified by the <code>type</code> keyword, only if <code>type</code> is explicitly defined within the same Schema Object. Other Schema Object constraints retain their defined behavior, and therefore may disallow the use of <code>null</code> as a value. A <code>false</code> value leaves the specified or default <code>type</code> unmodified. The default value is <code>false</code>.
      - name: writeOnly
        versions:
          - '3.0'
        logs:
          - version: '3.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Relevant only for Schema <code>"properties"</code> definitions. Declares the property as "write only". Therefore, it MAY be sent as part of a request but SHOULD NOT be sent as part of the response. If the property is marked as <code>writeOnly</code> being <code>true</code> and is in the <code>required</code> list, the <code>required</code> will take effect on the request only. A property MUST NOT be marked as both <code>readOnly</code> and <code>writeOnly</code> being <code>true</code>. Default value is <code>false</code>.
        mainType: boolean
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Relevant only for Schema <code>"properties"</code> definitions. Declares the property as "write only". Therefore, it MAY be sent as part of a request but SHOULD NOT be sent as part of the response. If the property is marked as <code>writeOnly</code> being <code>true</code> and is in the <code>required</code> list, the <code>required</code> will take effect on the request only. A property MUST NOT be marked as both <code>readOnly</code> and <code>writeOnly</code> being <code>true</code>. Default value is <code>false</code>.
      - name: deprecated
        versions:
          - '3.0'
        logs:
          - version: '3.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Specifies that a schema is deprecated and SHOULD be transitioned out of usage. Default value is <code>false</code>.
        mainType: boolean
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Specifies that a schema is deprecated and SHOULD be transitioned out of usage. Default value is <code>false</code>.
  - name: XML Object
    isRoot: false
    versions:
      - '2.0'
      - '3.0'
      - '3.1'
    logs:
      - version: '2.0'
        isExtensible: true
        description: <p>A metadata object that allows for more fine-tuned XML model definitions.</p><p>When using arrays, XML element names are <em>not</em> inferred (for singular/plural forms) and the <code>name</code> property should be used to add that information. See examples for expected behavior.</p>
      - version: '3.0'
        isExtensible: true
        description: |-
          <p>A metadata object that allows for more fine-tuned XML model definitions.</p><p>When using arrays, XML element names are <em>not</em> inferred (for singular/plural forms) and the <code>name</code> property SHOULD be used to add that information.
          See examples for expected behavior.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: |-
          <p>A metadata object that allows for more fine-tuned XML model definitions.</p><p>When using arrays, XML element names are <em>not</em> inferred (for singular/plural forms) and the <code>name</code> property SHOULD be used to add that information.
          See examples for expected behavior.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: |-
      <p>A metadata object that allows for more fine-tuned XML model definitions.</p><p>When using arrays, XML element names are <em>not</em> inferred (for singular/plural forms) and the <code>name</code> property SHOULD be used to add that information.
      See examples for expected behavior.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: XML Object (2.0, 3.0, 3.1)
    fields:
      - name: name
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Replaces the name of the element/attribute used for the described schema property. When defined within the Items Object (<code>items</code>), it will affect the name of the individual XML elements within the list. When defined alongside <code>type</code> being <code>array</code> (outside the <code>items</code>), it will affect the wrapping element and only if <code>wrapped</code> is <code>true</code>. If <code>wrapped</code> is <code>false</code>, it will be ignored.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Replaces the name of the element/attribute used for the described schema property. When defined within <code>items</code>, it will affect the name of the individual XML elements within the list. When defined alongside <code>type</code> being <code>array</code> (outside the <code>items</code>), it will affect the wrapping element and only if <code>wrapped</code> is <code>true</code>. If <code>wrapped</code> is <code>false</code>, it will be ignored.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Replaces the name of the element/attribute used for the described schema property. When defined within <code>items</code>, it will affect the name of the individual XML elements within the list. When defined alongside <code>type</code> being <code>array</code> (outside the <code>items</code>), it will affect the wrapping element and only if <code>wrapped</code> is <code>true</code>. If <code>wrapped</code> is <code>false</code>, it will be ignored.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Replaces the name of the element/attribute used for the described schema property. When defined within <code>items</code>, it will affect the name of the individual XML elements within the list. When defined alongside <code>type</code> being <code>array</code> (outside the <code>items</code>), it will affect the wrapping element and only if <code>wrapped</code> is <code>true</code>. If <code>wrapped</code> is <code>false</code>, it will be ignored.
      - name: namespace
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The URL of the namespace definition. Value SHOULD be in the form of a URL.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The URI of the namespace definition. Value MUST be in the form of an absolute URI.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The URI of the namespace definition. This MUST be in the form of an absolute URI.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The URI of the namespace definition. This MUST be in the form of an absolute URI.
      - name: prefix
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The prefix to be used for the <a href="#xmlName">name</a>.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The prefix to be used for the <a href="#xmlName">name</a>.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The prefix to be used for the <a href="#xmlName">name</a>.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The prefix to be used for the <a href="#xmlName">name</a>.
      - name: attribute
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Declares whether the property definition translates to an attribute instead of an element. Default value is <code>false</code>.
          - version: '3.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Declares whether the property definition translates to an attribute instead of an element. Default value is <code>false</code>.
          - version: '3.1'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Declares whether the property definition translates to an attribute instead of an element. Default value is <code>false</code>.
        mainType: boolean
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Declares whether the property definition translates to an attribute instead of an element. Default value is <code>false</code>.
      - name: wrapped
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: MAY be used only for an array definition. Signifies whether the array is wrapped (for example, <code>&lt;books&gt;&lt;book/&gt;&lt;book/&gt;&lt;/books&gt;</code>) or unwrapped (<code>&lt;book/&gt;&lt;book/&gt;</code>). Default value is <code>false</code>. The definition takes effect only when defined alongside <code>type</code> being <code>array</code> (outside the <code>items</code>).
          - version: '3.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: MAY be used only for an array definition. Signifies whether the array is wrapped (for example, <code>&lt;books&gt;&lt;book/&gt;&lt;book/&gt;&lt;/books&gt;</code>) or unwrapped (<code>&lt;book/&gt;&lt;book/&gt;</code>). Default value is <code>false</code>. The definition takes effect only when defined alongside <code>type</code> being <code>array</code> (outside the <code>items</code>).
          - version: '3.1'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: MAY be used only for an array definition. Signifies whether the array is wrapped (for example, <code>&lt;books&gt;&lt;book/&gt;&lt;book/&gt;&lt;/books&gt;</code>) or unwrapped (<code>&lt;book/&gt;&lt;book/&gt;</code>). Default value is <code>false</code>. The definition takes effect only when defined alongside <code>type</code> being <code>array</code> (outside the <code>items</code>).
        mainType: boolean
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: MAY be used only for an array definition. Signifies whether the array is wrapped (for example, <code>&lt;books&gt;&lt;book/&gt;&lt;book/&gt;&lt;/books&gt;</code>) or unwrapped (<code>&lt;book/&gt;&lt;book/&gt;</code>). Default value is <code>false</code>. The definition takes effect only when defined alongside <code>type</code> being <code>array</code> (outside the <code>items</code>).
      - name: ^x-
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the Swagger Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. See <a href="#vendorExtensions">Vendor Extensions</a> for further details.
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
  - name: Definitions Object
    isRoot: false
    versions:
      - '2.0'
    logs:
      - version: '2.0'
        isExtensible: false
        description: <p>An object to hold data types that can be consumed and produced by operations. These data types can be primitives, arrays or models.</p>
    isExtensible: false
    description: <p>An object to hold data types that can be consumed and produced by operations. These data types can be primitives, arrays or models.</p>
    fullName: Definitions Object (2.0)
    fields:
      - name: '{name}'
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Schema Object
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: A single definition, mapping a "name" to the schema it defines.
        mainType: Schema Object
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: A single definition, mapping a "name" to the schema it defines.
  - name: Parameters Definitions Object
    isRoot: false
    versions:
      - '2.0'
    logs:
      - version: '2.0'
        isExtensible: false
        description: <p>An object to hold parameters to be reused across operations. Parameter definitions can be referenced to the ones defined here.</p><p>This does <em>not</em> define global operation parameters.</p>
    isExtensible: false
    description: <p>An object to hold parameters to be reused across operations. Parameter definitions can be referenced to the ones defined here.</p><p>This does <em>not</em> define global operation parameters.</p>
    fullName: Parameters Definitions Object (2.0)
    fields:
      - name: '{name}'
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Parameter Object
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: A single parameter definition, mapping a "name" to the parameter it defines.
        mainType: Parameter Object
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: A single parameter definition, mapping a "name" to the parameter it defines.
  - name: Responses Definitions Object
    isRoot: false
    versions:
      - '2.0'
    logs:
      - version: '2.0'
        isExtensible: false
        description: <p>An object to hold responses to be reused across operations. Response definitions can be referenced to the ones defined here.</p><p>This does <em>not</em> define global operation responses.</p>
    isExtensible: false
    description: <p>An object to hold responses to be reused across operations. Response definitions can be referenced to the ones defined here.</p><p>This does <em>not</em> define global operation responses.</p>
    fullName: Responses Definitions Object (2.0)
    fields:
      - name: '{name}'
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Response Object
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: A single response definition, mapping a "name" to the response it defines.
        mainType: Response Object
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: A single response definition, mapping a "name" to the response it defines.
  - name: Security Definitions Object
    isRoot: false
    versions:
      - '2.0'
    logs:
      - version: '2.0'
        isExtensible: false
        description: <p>A declaration of the security schemes available to be used in the specification. This does not enforce the security schemes on the operations and only serves to provide the relevant details for each scheme.</p>
    isExtensible: false
    description: <p>A declaration of the security schemes available to be used in the specification. This does not enforce the security schemes on the operations and only serves to provide the relevant details for each scheme.</p>
    fullName: Security Definitions Object (2.0)
    fields:
      - name: '{name}'
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Security Scheme Object
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: A single security scheme definition, mapping a "name" to the scheme it defines.
        mainType: Security Scheme Object
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: A single security scheme definition, mapping a "name" to the scheme it defines.
  - name: Security Scheme Object
    isRoot: false
    versions:
      - '2.0'
      - '3.0'
      - '3.1'
    logs:
      - version: '2.0'
        isExtensible: true
        description: <p>Allows the definition of a security scheme that can be used by the operations. Supported schemes are basic authentication, an API key (either as a header or as a query parameter) and OAuth2's common flows (implicit, password, application and access code).</p>
      - version: '3.0'
        isExtensible: true
        description: |-
          <p>Defines a security scheme that can be used by the operations.
          Supported schemes are HTTP authentication, an API key (either as a header, a cookie parameter or as a query parameter), OAuth2's common flows (implicit, password, client credentials and authorization code) as defined in <a href="https://tools.ietf.org/html/rfc6749">RFC6749</a>, and <a href="https://tools.ietf.org/html/draft-ietf-oauth-discovery-06">OpenID Connect Discovery</a>.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: |-
          <p>Defines a security scheme that can be used by the operations.</p><p>Supported schemes are HTTP authentication, an API key (either as a header, a cookie parameter or as a query parameter), mutual TLS (use of a client certificate), OAuth2's common flows (implicit, password, client credentials and authorization code) as defined in <a href="https://tools.ietf.org/html/rfc6749">RFC6749</a>, and <a href="https://tools.ietf.org/html/draft-ietf-oauth-discovery-06">OpenID Connect Discovery</a>.
          Please note that as of 2020, the implicit flow is about to be deprecated by <a href="https://tools.ietf.org/html/draft-ietf-oauth-security-topics">OAuth 2.0 Security Best Current Practice</a>. Recommended for most use case is Authorization Code Grant flow with PKCE.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: |-
      <p>Defines a security scheme that can be used by the operations.</p><p>Supported schemes are HTTP authentication, an API key (either as a header, a cookie parameter or as a query parameter), mutual TLS (use of a client certificate), OAuth2's common flows (implicit, password, client credentials and authorization code) as defined in <a href="https://tools.ietf.org/html/rfc6749">RFC6749</a>, and <a href="https://tools.ietf.org/html/draft-ietf-oauth-discovery-06">OpenID Connect Discovery</a>.
      Please note that as of 2020, the implicit flow is about to be deprecated by <a href="https://tools.ietf.org/html/draft-ietf-oauth-security-topics">OAuth 2.0 Security Best Current Practice</a>. Recommended for most use case is Authorization Code Grant flow with PKCE.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Security Scheme Object (2.0, 3.0, 3.1)
    fields:
      - name: type
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The type of the security scheme. Valid values are <code>"basic"</code>, <code>"apiKey"</code> or <code>"oauth2"</code>.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The type of the security scheme. Valid values are <code>"apiKey"</code>, <code>"http"</code>, <code>"oauth2"</code>, <code>"openIdConnect"</code>.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The type of the security scheme. Valid values are <code>"apiKey"</code>, <code>"http"</code>, <code>"mutualTLS"</code>, <code>"oauth2"</code>, <code>"openIdConnect"</code>.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: The type of the security scheme. Valid values are <code>"apiKey"</code>, <code>"http"</code>, <code>"mutualTLS"</code>, <code>"oauth2"</code>, <code>"openIdConnect"</code>.
      - name: description
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A short description for security scheme.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A short description for security scheme.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A description for security scheme.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A description for security scheme.
      - name: name
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The name of the header or query parameter to be used.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The name of the header, query or cookie parameter to be used.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The name of the header, query or cookie parameter to be used.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: The name of the header, query or cookie parameter to be used.
      - name: in
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The location of the API key. Valid values are <code>"query"</code> or <code>"header"</code>.
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The location of the API key. Valid values are <code>"query"</code>, <code>"header"</code> or <code>"cookie"</code>.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The location of the API key. Valid values are <code>"query"</code>, <code>"header"</code> or <code>"cookie"</code>.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: The location of the API key. Valid values are <code>"query"</code>, <code>"header"</code> or <code>"cookie"</code>.
      - name: flow
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The flow used by the OAuth2 security scheme. Valid values are <code>"implicit"</code>, <code>"password"</code>, <code>"application"</code> or <code>"accessCode"</code>.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The flow used by the OAuth2 security scheme. Valid values are <code>"implicit"</code>, <code>"password"</code>, <code>"application"</code> or <code>"accessCode"</code>.
      - name: authorizationUrl
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The authorization URL to be used for this flow. This SHOULD be in the form of a URL.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The authorization URL to be used for this flow. This SHOULD be in the form of a URL.
      - name: tokenUrl
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The token URL to be used for this flow. This SHOULD be in the form of a URL.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The token URL to be used for this flow. This SHOULD be in the form of a URL.
      - name: scopes
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: Scopes Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The available scopes for the OAuth2 security scheme.
        mainType: Scopes Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The available scopes for the OAuth2 security scheme.
      - name: ^x-
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the Swagger Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. See <a href="#vendorExtensions">Vendor Extensions</a> for further details.
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
      - name: scheme
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The name of the HTTP Authorization scheme to be used in the <a href="https://tools.ietf.org/html/rfc7235#section-5.1">Authorization header as defined in RFC7235</a>.  The values used SHOULD be registered in the <a href="https://www.iana.org/assignments/http-authschemes/http-authschemes.xhtml">IANA Authentication Scheme registry</a>.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The name of the HTTP Authorization scheme to be used in the <a href="https://tools.ietf.org/html/rfc7235#section-5.1">Authorization header as defined in RFC7235</a>.  The values used SHOULD be registered in the <a href="https://www.iana.org/assignments/http-authschemes/http-authschemes.xhtml">IANA Authentication Scheme registry</a>.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: The name of the HTTP Authorization scheme to be used in the <a href="https://tools.ietf.org/html/rfc7235#section-5.1">Authorization header as defined in RFC7235</a>.  The values used SHOULD be registered in the <a href="https://www.iana.org/assignments/http-authschemes/http-authschemes.xhtml">IANA Authentication Scheme registry</a>.
      - name: bearerFormat
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A hint to the client to identify how the bearer token is formatted.  Bearer tokens are usually generated by an authorization server, so this information is primarily for documentation purposes.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A hint to the client to identify how the bearer token is formatted.  Bearer tokens are usually generated by an authorization server, so this information is primarily for documentation purposes.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A hint to the client to identify how the bearer token is formatted.  Bearer tokens are usually generated by an authorization server, so this information is primarily for documentation purposes.
      - name: flows
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: OAuth Flows Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: An object containing configuration information for the flow types supported.
          - version: '3.1'
            mainType: OAuth Flows Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: An object containing configuration information for the flow types supported.
        mainType: OAuth Flows Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: An object containing configuration information for the flow types supported.
      - name: openIdConnectUrl
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: OpenId Connect URL to discover OAuth2 configuration values. This MUST be in the form of a URL.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: OpenId Connect URL to discover OAuth2 configuration values. This MUST be in the form of a URL. The OpenID Connect standard requires the use of TLS.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: OpenId Connect URL to discover OAuth2 configuration values. This MUST be in the form of a URL. The OpenID Connect standard requires the use of TLS.
  - name: Scopes Object
    isRoot: false
    versions:
      - '2.0'
    logs:
      - version: '2.0'
        isExtensible: false
        description: <p>Lists the available scopes for an OAuth2 security scheme.</p>
    isExtensible: false
    description: <p>Lists the available scopes for an OAuth2 security scheme.</p>
    fullName: Scopes Object (2.0)
    fields:
      - name: '{name}'
        versions:
          - '2.0'
        logs:
          - version: '2.0'
            mainType: string
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Maps between a name of a scope to a short description of it (as the value of the property).
        mainType: string
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Maps between a name of a scope to a short description of it (as the value of the property).
  - name: Security Requirement Object
    isRoot: false
    versions:
      - '2.0'
      - '3.0'
      - '3.1'
    logs:
      - version: '2.0'
        isExtensible: false
        description: <p>Lists the required security schemes to execute this operation. The object can have multiple security schemes declared in it which are all required (that is, there is a logical AND between the schemes).</p><p>The name used for each property MUST correspond to a security scheme declared in the <a href="#securityDefinitionsObject">Security Definitions</a>.</p>
      - version: '3.0'
        isExtensible: false
        description: |-
          <p>Lists the required security schemes to execute this operation.
          The name used for each property MUST correspond to a security scheme declared in the <a href="#componentsSecuritySchemes">Security Schemes</a> under the <a href="#componentsObject">Components Object</a>.</p><p>Security Requirement Objects that contain multiple schemes require that all schemes MUST be satisfied for a request to be authorized.
          This enables support for scenarios where multiple query parameters or HTTP headers are required to convey security information.</p><p>When a list of Security Requirement Objects is defined on the <a href="#oasObject">OpenAPI Object</a> or <a href="#operationObject">Operation Object</a>, only one of the Security Requirement Objects in the list needs to be satisfied to authorize the request.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: false
        description: |-
          <p>Lists the required security schemes to execute this operation.
          The name used for each property MUST correspond to a security scheme declared in the <a href="#componentsSecuritySchemes">Security Schemes</a> under the <a href="#componentsObject">Components Object</a>.</p><p>Security Requirement Objects that contain multiple schemes require that all schemes MUST be satisfied for a request to be authorized.
          This enables support for scenarios where multiple query parameters or HTTP headers are required to convey security information.</p><p>When a list of Security Requirement Objects is defined on the <a href="#oasObject">OpenAPI Object</a> or <a href="#operationObject">Operation Object</a>, only one of the Security Requirement Objects in the list needs to be satisfied to authorize the request.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: false
    description: |-
      <p>Lists the required security schemes to execute this operation.
      The name used for each property MUST correspond to a security scheme declared in the <a href="#componentsSecuritySchemes">Security Schemes</a> under the <a href="#componentsObject">Components Object</a>.</p><p>Security Requirement Objects that contain multiple schemes require that all schemes MUST be satisfied for a request to be authorized.
      This enables support for scenarios where multiple query parameters or HTTP headers are required to convey security information.</p><p>When a list of Security Requirement Objects is defined on the <a href="#oasObject">OpenAPI Object</a> or <a href="#operationObject">Operation Object</a>, only one of the Security Requirement Objects in the list needs to be satisfied to authorize the request.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Security Requirement Object (2.0, 3.0, 3.1)
    fields:
      - name: '{name}'
        versions:
          - '2.0'
          - '3.0'
          - '3.1'
        logs:
          - version: '2.0'
            mainType: string
            parentType: array
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Each name must correspond to a security scheme which is declared in the <a href="#securityDefinitions">Security Definitions</a>. If the security scheme is of type <code>"oauth2"</code>, then the value is a list of scope names required for the execution. For other security scheme types, the array MUST be empty.
          - version: '3.0'
            mainType: string
            parentType: array
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Each name MUST correspond to a security scheme which is declared in the <a href="#componentsSecuritySchemes">Security Schemes</a> under the <a href="#componentsObject">Components Object</a>. If the security scheme is of type <code>"oauth2"</code> or <code>"openIdConnect"</code>, then the value is a list of scope names required for the execution, and the list MAY be empty if authorization does not require a specified scope. For other security scheme types, the array MUST be empty.
          - version: '3.1'
            mainType: string
            parentType: array
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Each name MUST correspond to a security scheme which is declared in the <a href="#componentsSecuritySchemes">Security Schemes</a> under the <a href="#componentsObject">Components Object</a>. If the security scheme is of type <code>"oauth2"</code> or <code>"openIdConnect"</code>, then the value is a list of scope names required for the execution, and the list MAY be empty if authorization does not require a specified scope. For other security scheme types, the array MAY contain a list of role names which are required for the execution, but are not otherwise defined or exchanged in-band.
        mainType: string
        parentType: array
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Each name MUST correspond to a security scheme which is declared in the <a href="#componentsSecuritySchemes">Security Schemes</a> under the <a href="#componentsObject">Components Object</a>. If the security scheme is of type <code>"oauth2"</code> or <code>"openIdConnect"</code>, then the value is a list of scope names required for the execution, and the list MAY be empty if authorization does not require a specified scope. For other security scheme types, the array MAY contain a list of role names which are required for the execution, but are not otherwise defined or exchanged in-band.
  - name: Server Object
    isRoot: false
    versions:
      - '3.0'
      - '3.1'
    logs:
      - version: '3.0'
        isExtensible: true
        description: <p>An object representing a Server.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: <p>An object representing a Server.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: <p>An object representing a Server.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Server Object (3.0, 3.1)
    fields:
      - name: url
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: A URL to the target host.  This URL supports Server Variables and MAY be relative, to indicate that the host location is relative to the location where the OpenAPI document is being served. Variable substitutions will be made when a variable is named in <code>{</code>brackets<code>}</code>.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: A URL to the target host.  This URL supports Server Variables and MAY be relative, to indicate that the host location is relative to the location where the OpenAPI document is being served. Variable substitutions will be made when a variable is named in <code>{</code>brackets<code>}</code>.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: A URL to the target host.  This URL supports Server Variables and MAY be relative, to indicate that the host location is relative to the location where the OpenAPI document is being served. Variable substitutions will be made when a variable is named in <code>{</code>brackets<code>}</code>.
      - name: description
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An optional string describing the host designated by the URL.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An optional string describing the host designated by the URL.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: An optional string describing the host designated by the URL.
      - name: variables
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Server Variable Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A map between a variable name and its value.  The value is used for substitution in the server's URL template.
          - version: '3.1'
            mainType: Server Variable Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A map between a variable name and its value.  The value is used for substitution in the server's URL template.
        mainType: Server Variable Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A map between a variable name and its value.  The value is used for substitution in the server's URL template.
      - name: ^x-
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
  - name: Server Variable Object
    isRoot: false
    versions:
      - '3.0'
      - '3.1'
    logs:
      - version: '3.0'
        isExtensible: true
        description: <p>An object representing a Server Variable for server URL template substitution.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: <p>An object representing a Server Variable for server URL template substitution.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: <p>An object representing a Server Variable for server URL template substitution.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Server Variable Object (3.0, 3.1)
    fields:
      - name: enum
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An enumeration of string values to be used if the substitution options are from a limited set. The array SHOULD NOT be empty.
          - version: '3.1'
            mainType: string
            parentType: array
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An enumeration of string values to be used if the substitution options are from a limited set. The array MUST NOT be empty.
        mainType: string
        parentType: array
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: An enumeration of string values to be used if the substitution options are from a limited set. The array MUST NOT be empty.
      - name: default
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The default value to use for substitution, which SHALL be sent if an alternate value is <em>not</em> supplied. Note this behavior is different than the <a href="#schemaObject">Schema Object's</a> treatment of default values, because in those cases parameter values are optional. If the <a href="#serverVariableEnum"><code>enum</code></a> is defined, the value SHOULD exist in the enum's values.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The default value to use for substitution, which SHALL be sent if an alternate value is <em>not</em> supplied. Note this behavior is different than the <a href="#schemaObject">Schema Object's</a> treatment of default values, because in those cases parameter values are optional. If the <a href="#serverVariableEnum"><code>enum</code></a> is defined, the value MUST exist in the enum's values.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: The default value to use for substitution, which SHALL be sent if an alternate value is <em>not</em> supplied. Note this behavior is different than the <a href="#schemaObject">Schema Object's</a> treatment of default values, because in those cases parameter values are optional. If the <a href="#serverVariableEnum"><code>enum</code></a> is defined, the value MUST exist in the enum's values.
      - name: description
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An optional description for the server variable.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An optional description for the server variable.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: An optional description for the server variable.
      - name: ^x-
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
  - name: Components Object
    isRoot: false
    versions:
      - '3.0'
      - '3.1'
    logs:
      - version: '3.0'
        isExtensible: true
        description: |-
          <p>Holds a set of reusable objects for different aspects of the OAS.
          All objects defined within the components object will have no effect on the API unless they are explicitly referenced from properties outside the components object.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: |-
          <p>Holds a set of reusable objects for different aspects of the OAS.
          All objects defined within the components object will have no effect on the API unless they are explicitly referenced from properties outside the components object.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: |-
      <p>Holds a set of reusable objects for different aspects of the OAS.
      All objects defined within the components object will have no effect on the API unless they are explicitly referenced from properties outside the components object.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Components Object (3.0, 3.1)
    fields:
      - name: schemas
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Schema Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: An object to hold reusable <a href="#schemaObject">Schema Objects</a>.
          - version: '3.1'
            mainType: Schema Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An object to hold reusable <a href="#schemaObject">Schema Objects</a>.
        mainType: Schema Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: An object to hold reusable <a href="#schemaObject">Schema Objects</a>.
      - name: responses
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Response Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: An object to hold reusable <a href="#responseObject">Response Objects</a>.
          - version: '3.1'
            mainType: Response Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: An object to hold reusable <a href="#responseObject">Response Objects</a>.
        mainType: Response Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: true
        description: An object to hold reusable <a href="#responseObject">Response Objects</a>.
      - name: parameters
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Parameter Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: An object to hold reusable <a href="#parameterObject">Parameter Objects</a>.
          - version: '3.1'
            mainType: Parameter Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: An object to hold reusable <a href="#parameterObject">Parameter Objects</a>.
        mainType: Parameter Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: true
        description: An object to hold reusable <a href="#parameterObject">Parameter Objects</a>.
      - name: examples
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Example Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: An object to hold reusable <a href="#exampleObject">Example Objects</a>.
          - version: '3.1'
            mainType: Example Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: An object to hold reusable <a href="#exampleObject">Example Objects</a>.
        mainType: Example Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: true
        description: An object to hold reusable <a href="#exampleObject">Example Objects</a>.
      - name: requestBodies
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Request Body Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: An object to hold reusable <a href="#requestBodyObject">Request Body Objects</a>.
          - version: '3.1'
            mainType: Request Body Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: An object to hold reusable <a href="#requestBodyObject">Request Body Objects</a>.
        mainType: Request Body Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: true
        description: An object to hold reusable <a href="#requestBodyObject">Request Body Objects</a>.
      - name: headers
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Header Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: An object to hold reusable <a href="#headerObject">Header Objects</a>.
          - version: '3.1'
            mainType: Header Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: An object to hold reusable <a href="#headerObject">Header Objects</a>.
        mainType: Header Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: true
        description: An object to hold reusable <a href="#headerObject">Header Objects</a>.
      - name: securitySchemes
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Security Scheme Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: An object to hold reusable <a href="#securitySchemeObject">Security Scheme Objects</a>.
          - version: '3.1'
            mainType: Security Scheme Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: An object to hold reusable <a href="#securitySchemeObject">Security Scheme Objects</a>.
        mainType: Security Scheme Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: true
        description: An object to hold reusable <a href="#securitySchemeObject">Security Scheme Objects</a>.
      - name: links
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Link Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: An object to hold reusable <a href="#linkObject">Link Objects</a>.
          - version: '3.1'
            mainType: Link Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: An object to hold reusable <a href="#linkObject">Link Objects</a>.
        mainType: Link Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: true
        description: An object to hold reusable <a href="#linkObject">Link Objects</a>.
      - name: callbacks
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Callback Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: An object to hold reusable <a href="#callbackObject">Callback Objects</a>.
          - version: '3.1'
            mainType: Callback Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: An object to hold reusable <a href="#callbackObject">Callback Objects</a>.
        mainType: Callback Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: true
        description: An object to hold reusable <a href="#callbackObject">Callback Objects</a>.
      - name: ^x-
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
      - name: pathItems
        versions:
          - '3.1'
        logs:
          - version: '3.1'
            mainType: Path Item Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: An object to hold reusable <a href="#pathItemObject">Path Item Object</a>.
        mainType: Path Item Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: true
        description: An object to hold reusable <a href="#pathItemObject">Path Item Object</a>.
  - name: Request Body Object
    isRoot: false
    versions:
      - '3.0'
      - '3.1'
    logs:
      - version: '3.0'
        isExtensible: true
        description: <p>Describes a single request body.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: <p>Describes a single request body.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: <p>Describes a single request body.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Request Body Object (3.0, 3.1)
    fields:
      - name: description
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A brief description of the request body.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A brief description of the request body.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A brief description of the request body.
      - name: content
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Media Type Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The content of the request body. The key is a media type or <a href="https://tools.ietf.org/html/rfc7231#appendix-D">media type range</a> and the value describes it.  For requests that match multiple keys, only the most specific key is applicable. e.g. text/plain overrides text/*
          - version: '3.1'
            mainType: Media Type Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The content of the request body. The key is a media type or <a href="https://tools.ietf.org/html/rfc7231#appendix-D">media type range</a> and the value describes it.  For requests that match multiple keys, only the most specific key is applicable. e.g. text/plain overrides text/*
        mainType: Media Type Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: The content of the request body. The key is a media type or <a href="https://tools.ietf.org/html/rfc7231#appendix-D">media type range</a> and the value describes it.  For requests that match multiple keys, only the most specific key is applicable. e.g. text/plain overrides text/*
      - name: required
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Determines if the request body is required in the request. Defaults to <code>false</code>.
          - version: '3.1'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Determines if the request body is required in the request. Defaults to <code>false</code>.
        mainType: boolean
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Determines if the request body is required in the request. Defaults to <code>false</code>.
      - name: ^x-
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
  - name: Media Type Object
    isRoot: false
    versions:
      - '3.0'
      - '3.1'
    logs:
      - version: '3.0'
        isExtensible: true
        description: <p>Each Media Type Object provides schema and examples for the media type identified by its key.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: <p>Each Media Type Object provides schema and examples for the media type identified by its key.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: <p>Each Media Type Object provides schema and examples for the media type identified by its key.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Media Type Object (3.0, 3.1)
    fields:
      - name: schema
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Schema Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: The schema defining the content of the request, response, or parameter.
          - version: '3.1'
            mainType: Schema Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The schema defining the content of the request, response, or parameter.
        mainType: Schema Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The schema defining the content of the request, response, or parameter.
      - name: example
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Example of the media type.  The example object SHOULD be in the correct format as specified by the media type.  The <code>example</code> field is mutually exclusive of the <code>examples</code> field.  Furthermore, if referencing a <code>schema</code> which contains an example, the <code>example</code> value SHALL <em>override</em> the example provided by the schema.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Example of the media type.  The example object SHOULD be in the correct format as specified by the media type.  The <code>example</code> field is mutually exclusive of the <code>examples</code> field.  Furthermore, if referencing a <code>schema</code> which contains an example, the <code>example</code> value SHALL <em>override</em> the example provided by the schema.
        mainType: Any
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Example of the media type.  The example object SHOULD be in the correct format as specified by the media type.  The <code>example</code> field is mutually exclusive of the <code>examples</code> field.  Furthermore, if referencing a <code>schema</code> which contains an example, the <code>example</code> value SHALL <em>override</em> the example provided by the schema.
      - name: examples
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Example Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: Examples of the media type.  Each example object SHOULD  match the media type and specified schema if present.  The <code>examples</code> field is mutually exclusive of the <code>example</code> field.  Furthermore, if referencing a <code>schema</code> which contains an example, the <code>examples</code> value SHALL <em>override</em> the example provided by the schema.
          - version: '3.1'
            mainType: Example Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: Examples of the media type.  Each example object SHOULD  match the media type and specified schema if present.  The <code>examples</code> field is mutually exclusive of the <code>example</code> field.  Furthermore, if referencing a <code>schema</code> which contains an example, the <code>examples</code> value SHALL <em>override</em> the example provided by the schema.
        mainType: Example Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: true
        description: Examples of the media type.  Each example object SHOULD  match the media type and specified schema if present.  The <code>examples</code> field is mutually exclusive of the <code>example</code> field.  Furthermore, if referencing a <code>schema</code> which contains an example, the <code>examples</code> value SHALL <em>override</em> the example provided by the schema.
      - name: encoding
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Encoding Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A map between a property name and its encoding information. The key, being the property name, MUST exist in the schema as a property. The encoding object SHALL only apply to <code>requestBody</code> objects when the media type is <code>multipart</code> or <code>application/x-www-form-urlencoded</code>.
          - version: '3.1'
            mainType: Encoding Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A map between a property name and its encoding information. The key, being the property name, MUST exist in the schema as a property. The encoding object SHALL only apply to <code>requestBody</code> objects when the media type is <code>multipart</code> or <code>application/x-www-form-urlencoded</code>.
        mainType: Encoding Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A map between a property name and its encoding information. The key, being the property name, MUST exist in the schema as a property. The encoding object SHALL only apply to <code>requestBody</code> objects when the media type is <code>multipart</code> or <code>application/x-www-form-urlencoded</code>.
      - name: ^x-
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
  - name: Encoding Object
    isRoot: false
    versions:
      - '3.0'
      - '3.1'
    logs:
      - version: '3.0'
        isExtensible: true
        description: <p>A single encoding definition applied to a single schema property.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: <p>A single encoding definition applied to a single schema property.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: <p>A single encoding definition applied to a single schema property.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Encoding Object (3.0, 3.1)
    fields:
      - name: contentType
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: 'The Content-Type for encoding a specific property. Default value depends on the property type: for <code>string</code> with <code>format</code> being <code>binary</code> – <code>application/octet-stream</code>; for other primitive types – <code>text/plain</code>; for <code>object</code> - <code>application/json</code>; for <code>array</code> – the default is defined based on the inner type. The value can be a specific media type (e.g. <code>application/json</code>), a wildcard media type (e.g. <code>image/*</code>), or a comma-separated list of the two types.'
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: 'The Content-Type for encoding a specific property. Default value depends on the property type: for <code>object</code> - <code>application/json</code>;  for <code>array</code> – the default is defined based on the inner type; for all other cases the default is <code>application/octet-stream</code>. The value can be a specific media type (e.g. <code>application/json</code>), a wildcard media type (e.g. <code>image/*</code>), or a comma-separated list of the two types.'
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: 'The Content-Type for encoding a specific property. Default value depends on the property type: for <code>object</code> - <code>application/json</code>;  for <code>array</code> – the default is defined based on the inner type; for all other cases the default is <code>application/octet-stream</code>. The value can be a specific media type (e.g. <code>application/json</code>), a wildcard media type (e.g. <code>image/*</code>), or a comma-separated list of the two types.'
      - name: headers
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Header Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: A map allowing additional information to be provided as headers, for example <code>Content-Disposition</code>.  <code>Content-Type</code> is described separately and SHALL be ignored in this section. This property SHALL be ignored if the request body media type is not a <code>multipart</code>.
          - version: '3.1'
            mainType: Header Object
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: true
            description: A map allowing additional information to be provided as headers, for example <code>Content-Disposition</code>.  <code>Content-Type</code> is described separately and SHALL be ignored in this section. This property SHALL be ignored if the request body media type is not a <code>multipart</code>.
        mainType: Header Object
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: true
        description: A map allowing additional information to be provided as headers, for example <code>Content-Disposition</code>.  <code>Content-Type</code> is described separately and SHALL be ignored in this section. This property SHALL be ignored if the request body media type is not a <code>multipart</code>.
      - name: style
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Describes how a specific property value will be serialized depending on its type.  See <a href="#parameterObject">Parameter Object</a> for details on the <a href="#parameterStyle"><code>style</code></a> property. The behavior follows the same values as <code>query</code> parameters, including default values. This property SHALL be ignored if the request body media type is not <code>application/x-www-form-urlencoded</code>.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Describes how a specific property value will be serialized depending on its type.  See <a href="#parameterObject">Parameter Object</a> for details on the <a href="#parameterStyle"><code>style</code></a> property. The behavior follows the same values as <code>query</code> parameters, including default values. This property SHALL be ignored if the request body media type is not <code>application/x-www-form-urlencoded</code> or <code>multipart/form-data</code>. If a value is explicitly defined, then the value of <a href="#encodingContentType"><code>contentType</code></a> (implicit or explicit) SHALL be ignored.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Describes how a specific property value will be serialized depending on its type.  See <a href="#parameterObject">Parameter Object</a> for details on the <a href="#parameterStyle"><code>style</code></a> property. The behavior follows the same values as <code>query</code> parameters, including default values. This property SHALL be ignored if the request body media type is not <code>application/x-www-form-urlencoded</code> or <code>multipart/form-data</code>. If a value is explicitly defined, then the value of <a href="#encodingContentType"><code>contentType</code></a> (implicit or explicit) SHALL be ignored.
      - name: explode
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: When this is true, property values of type <code>array</code> or <code>object</code> generate separate parameters for each value of the array, or key-value-pair of the map.  For other types of properties this property has no effect. When <a href="#encodingStyle"><code>style</code></a> is <code>form</code>, the default value is <code>true</code>. For all other styles, the default value is <code>false</code>. This property SHALL be ignored if the request body media type is not <code>application/x-www-form-urlencoded</code>.
          - version: '3.1'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: When this is true, property values of type <code>array</code> or <code>object</code> generate separate parameters for each value of the array, or key-value-pair of the map.  For other types of properties this property has no effect. When <a href="#encodingStyle"><code>style</code></a> is <code>form</code>, the default value is <code>true</code>. For all other styles, the default value is <code>false</code>. This property SHALL be ignored if the request body media type is not <code>application/x-www-form-urlencoded</code> or <code>multipart/form-data</code>. If a value is explicitly defined, then the value of <a href="#encodingContentType"><code>contentType</code></a> (implicit or explicit) SHALL be ignored.
        mainType: boolean
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: When this is true, property values of type <code>array</code> or <code>object</code> generate separate parameters for each value of the array, or key-value-pair of the map.  For other types of properties this property has no effect. When <a href="#encodingStyle"><code>style</code></a> is <code>form</code>, the default value is <code>true</code>. For all other styles, the default value is <code>false</code>. This property SHALL be ignored if the request body media type is not <code>application/x-www-form-urlencoded</code> or <code>multipart/form-data</code>. If a value is explicitly defined, then the value of <a href="#encodingContentType"><code>contentType</code></a> (implicit or explicit) SHALL be ignored.
      - name: allowReserved
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Determines whether the parameter value SHOULD allow reserved characters, as defined by <a href="https://tools.ietf.org/html/rfc3986#section-2.2">RFC3986</a> <code>:/?#[]@!$&amp;'()*+,;=</code> to be included without percent-encoding. The default value is <code>false</code>. This property SHALL be ignored if the request body media type is not <code>application/x-www-form-urlencoded</code>.
          - version: '3.1'
            mainType: boolean
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Determines whether the parameter value SHOULD allow reserved characters, as defined by <a href="https://tools.ietf.org/html/rfc3986#section-2.2">RFC3986</a> <code>:/?#[]@!$&amp;'()*+,;=</code> to be included without percent-encoding. The default value is <code>false</code>. This property SHALL be ignored if the request body media type is not <code>application/x-www-form-urlencoded</code> or <code>multipart/form-data</code>. If a value is explicitly defined, then the value of <a href="#encodingContentType"><code>contentType</code></a> (implicit or explicit) SHALL be ignored.
        mainType: boolean
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Determines whether the parameter value SHOULD allow reserved characters, as defined by <a href="https://tools.ietf.org/html/rfc3986#section-2.2">RFC3986</a> <code>:/?#[]@!$&amp;'()*+,;=</code> to be included without percent-encoding. The default value is <code>false</code>. This property SHALL be ignored if the request body media type is not <code>application/x-www-form-urlencoded</code> or <code>multipart/form-data</code>. If a value is explicitly defined, then the value of <a href="#encodingContentType"><code>contentType</code></a> (implicit or explicit) SHALL be ignored.
      - name: ^x-
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
  - name: Callback Object
    isRoot: false
    versions:
      - '3.0'
      - '3.1'
    logs:
      - version: '3.0'
        isExtensible: true
        description: |-
          <p>A map of possible out-of band callbacks related to the parent operation.
          Each value in the map is a <a href="#pathItemObject">Path Item Object</a> that describes a set of requests that may be initiated by the API provider and the expected responses.
          The key value used to identify the path item object is an expression, evaluated at runtime, that identifies a URL to use for the callback operation.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: |-
          <p>A map of possible out-of band callbacks related to the parent operation.
          Each value in the map is a <a href="#pathItemObject">Path Item Object</a> that describes a set of requests that may be initiated by the API provider and the expected responses.
          The key value used to identify the path item object is an expression, evaluated at runtime, that identifies a URL to use for the callback operation.</p><p>To describe incoming requests from the API provider independent from another API call, use the <a href="#oasWebhooks"><code>webhooks</code></a> field.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: |-
      <p>A map of possible out-of band callbacks related to the parent operation.
      Each value in the map is a <a href="#pathItemObject">Path Item Object</a> that describes a set of requests that may be initiated by the API provider and the expected responses.
      The key value used to identify the path item object is an expression, evaluated at runtime, that identifies a URL to use for the callback operation.</p><p>To describe incoming requests from the API provider independent from another API call, use the <a href="#oasWebhooks"><code>webhooks</code></a> field.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Callback Object (3.0, 3.1)
    fields:
      - name: '{expression}'
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Path Item Object
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: A Path Item Object used to define a callback request and expected responses.  A <a href="../examples/v3.0/callback-example.yaml">complete example</a> is available.
          - version: '3.1'
            mainType: Path Item Object
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: true
            description: A Path Item Object, or a reference to one, used to define a callback request and expected responses.  A <a href="../examples/v3.0/callback-example.yaml">complete example</a> is available.
        mainType: Path Item Object
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: true
        description: A Path Item Object, or a reference to one, used to define a callback request and expected responses.  A <a href="../examples/v3.0/callback-example.yaml">complete example</a> is available.
      - name: ^x-
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
  - name: Link Object
    isRoot: false
    versions:
      - '3.0'
      - '3.1'
    logs:
      - version: '3.0'
        isExtensible: true
        description: |-
          <p>The <code>Link object</code> represents a possible design-time link for a response.
          The presence of a link does not guarantee the caller's ability to successfully invoke it, rather it provides a known relationship and traversal mechanism between responses and other operations.</p><p>Unlike <em>dynamic</em> links (i.e. links provided <strong>in</strong> the response payload), the OAS linking mechanism does not require link information in the runtime response.</p><p>For computing links, and providing instructions to execute them, a <a href="#runtimeExpression">runtime expression</a> is used for accessing values in an operation and using them as parameters while invoking the linked operation.  </p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: |-
          <p>The <code>Link object</code> represents a possible design-time link for a response.
          The presence of a link does not guarantee the caller's ability to successfully invoke it, rather it provides a known relationship and traversal mechanism between responses and other operations.</p><p>Unlike <em>dynamic</em> links (i.e. links provided <strong>in</strong> the response payload), the OAS linking mechanism does not require link information in the runtime response.</p><p>For computing links, and providing instructions to execute them, a <a href="#runtimeExpression">runtime expression</a> is used for accessing values in an operation and using them as parameters while invoking the linked operation.  </p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: |-
      <p>The <code>Link object</code> represents a possible design-time link for a response.
      The presence of a link does not guarantee the caller's ability to successfully invoke it, rather it provides a known relationship and traversal mechanism between responses and other operations.</p><p>Unlike <em>dynamic</em> links (i.e. links provided <strong>in</strong> the response payload), the OAS linking mechanism does not require link information in the runtime response.</p><p>For computing links, and providing instructions to execute them, a <a href="#runtimeExpression">runtime expression</a> is used for accessing values in an operation and using them as parameters while invoking the linked operation.  </p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Link Object (3.0, 3.1)
    fields:
      - name: operationRef
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A relative or absolute URI reference to an OAS operation. This field is mutually exclusive of the <code>operationId</code> field, and MUST point to an <a href="#operationObject">Operation Object</a>. Relative <code>operationRef</code> values MAY be used to locate an existing <a href="#operationObject">Operation Object</a> in the OpenAPI definition.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A relative or absolute URI reference to an OAS operation. This field is mutually exclusive of the <code>operationId</code> field, and MUST point to an <a href="#operationObject">Operation Object</a>. Relative <code>operationRef</code> values MAY be used to locate an existing <a href="#operationObject">Operation Object</a> in the OpenAPI definition. See the rules for resolving <a href="#relativeReferencesURI">Relative References</a>.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A relative or absolute URI reference to an OAS operation. This field is mutually exclusive of the <code>operationId</code> field, and MUST point to an <a href="#operationObject">Operation Object</a>. Relative <code>operationRef</code> values MAY be used to locate an existing <a href="#operationObject">Operation Object</a> in the OpenAPI definition. See the rules for resolving <a href="#relativeReferencesURI">Relative References</a>.
      - name: operationId
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The name of an <em>existing</em>, resolvable OAS operation, as defined with a unique <code>operationId</code>.  This field is mutually exclusive of the <code>operationRef</code> field.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The name of an <em>existing</em>, resolvable OAS operation, as defined with a unique <code>operationId</code>.  This field is mutually exclusive of the <code>operationRef</code> field.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The name of an <em>existing</em>, resolvable OAS operation, as defined with a unique <code>operationId</code>.  This field is mutually exclusive of the <code>operationRef</code> field.
      - name: parameters
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Any
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A map representing parameters to pass to an operation as specified with <code>operationId</code> or identified via <code>operationRef</code>. The key is the parameter name to be used, whereas the value can be a constant or an expression to be evaluated and passed to the linked operation.  The parameter name can be qualified using the <a href="#parameterIn">parameter location</a> <code>[{in}.]{name}</code> for operations that use the same parameter name in different locations (e.g. path.id).
          - version: '3.1'
            mainType: Any
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A map representing parameters to pass to an operation as specified with <code>operationId</code> or identified via <code>operationRef</code>. The key is the parameter name to be used, whereas the value can be a constant or an expression to be evaluated and passed to the linked operation.  The parameter name can be qualified using the <a href="#parameterIn">parameter location</a> <code>[{in}.]{name}</code> for operations that use the same parameter name in different locations (e.g. path.id).
        mainType: Any
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A map representing parameters to pass to an operation as specified with <code>operationId</code> or identified via <code>operationRef</code>. The key is the parameter name to be used, whereas the value can be a constant or an expression to be evaluated and passed to the linked operation.  The parameter name can be qualified using the <a href="#parameterIn">parameter location</a> <code>[{in}.]{name}</code> for operations that use the same parameter name in different locations (e.g. path.id).
      - name: requestBody
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A literal value or <a href="#runtimeExpression">{expression}</a> to use as a request body when calling the target operation.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A literal value or <a href="#runtimeExpression">{expression}</a> to use as a request body when calling the target operation.
        mainType: Any
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A literal value or <a href="#runtimeExpression">{expression}</a> to use as a request body when calling the target operation.
      - name: description
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A description of the link.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A description of the link.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A description of the link.
      - name: server
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Server Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A server object to be used by the target operation.
          - version: '3.1'
            mainType: Server Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: A server object to be used by the target operation.
        mainType: Server Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: A server object to be used by the target operation.
      - name: ^x-
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
  - name: Discriminator Object
    isRoot: false
    versions:
      - '3.0'
      - '3.1'
    logs:
      - version: '3.0'
        isExtensible: false
        description: <p>When request bodies or response payloads may be one of a number of different schemas, a <code>discriminator</code> object can be used to aid in serialization, deserialization, and validation.  The discriminator is a specific object in a schema which is used to inform the consumer of the specification of an alternative schema based on the value associated with it.</p><p>When using the discriminator, <em>inline</em> schemas will not be considered.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: <p>When request bodies or response payloads may be one of a number of different schemas, a <code>discriminator</code> object can be used to aid in serialization, deserialization, and validation.  The discriminator is a specific object in a schema which is used to inform the consumer of the document of an alternative schema based on the value associated with it.</p><p>When using the discriminator, <em>inline</em> schemas will not be considered.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: <p>When request bodies or response payloads may be one of a number of different schemas, a <code>discriminator</code> object can be used to aid in serialization, deserialization, and validation.  The discriminator is a specific object in a schema which is used to inform the consumer of the document of an alternative schema based on the value associated with it.</p><p>When using the discriminator, <em>inline</em> schemas will not be considered.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: Discriminator Object (3.0, 3.1)
    fields:
      - name: propertyName
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The name of the property in the payload that will hold the discriminator value.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The name of the property in the payload that will hold the discriminator value.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: The name of the property in the payload that will hold the discriminator value.
      - name: mapping
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An object to hold mappings between payload values and schema names or references.
          - version: '3.1'
            mainType: string
            parentType: map
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: An object to hold mappings between payload values and schema names or references.
        mainType: string
        parentType: map
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: An object to hold mappings between payload values and schema names or references.
      - name: ^x-
        versions:
          - '3.1'
        logs:
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
  - name: OAuth Flows Object
    isRoot: false
    versions:
      - '3.0'
      - '3.1'
    logs:
      - version: '3.0'
        isExtensible: true
        description: <p>Allows configuration of the supported OAuth Flows.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: <p>Allows configuration of the supported OAuth Flows.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: <p>Allows configuration of the supported OAuth Flows.</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: OAuth Flows Object (3.0, 3.1)
    fields:
      - name: implicit
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: OAuth Flow Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Configuration for the OAuth Implicit flow
          - version: '3.1'
            mainType: OAuth Flow Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Configuration for the OAuth Implicit flow
        mainType: OAuth Flow Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Configuration for the OAuth Implicit flow
      - name: password
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: OAuth Flow Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Configuration for the OAuth Resource Owner Password flow
          - version: '3.1'
            mainType: OAuth Flow Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Configuration for the OAuth Resource Owner Password flow
        mainType: OAuth Flow Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Configuration for the OAuth Resource Owner Password flow
      - name: clientCredentials
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: OAuth Flow Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Configuration for the OAuth Client Credentials flow.  Previously called <code>application</code> in OpenAPI 2.0.
          - version: '3.1'
            mainType: OAuth Flow Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Configuration for the OAuth Client Credentials flow.  Previously called <code>application</code> in OpenAPI 2.0.
        mainType: OAuth Flow Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Configuration for the OAuth Client Credentials flow.  Previously called <code>application</code> in OpenAPI 2.0.
      - name: authorizationCode
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: OAuth Flow Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Configuration for the OAuth Authorization Code flow.  Previously called <code>accessCode</code> in OpenAPI 2.0.
          - version: '3.1'
            mainType: OAuth Flow Object
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: Configuration for the OAuth Authorization Code flow.  Previously called <code>accessCode</code> in OpenAPI 2.0.
        mainType: OAuth Flow Object
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: Configuration for the OAuth Authorization Code flow.  Previously called <code>accessCode</code> in OpenAPI 2.0.
      - name: ^x-
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
  - name: OAuth Flow Object
    isRoot: false
    versions:
      - '3.0'
      - '3.1'
    logs:
      - version: '3.0'
        isExtensible: true
        description: <p>Configuration details for a supported OAuth Flow</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
      - version: '3.1'
        isExtensible: true
        description: <p>Configuration details for a supported OAuth Flow</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    isExtensible: true
    description: <p>Configuration details for a supported OAuth Flow</p><p>In the following description, if a field is not explicitly <strong>REQUIRED</strong> or described with a MUST or SHALL, it can be considered OPTIONAL.</p>
    fullName: OAuth Flow Object (3.0, 3.1)
    fields:
      - name: authorizationUrl
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The authorization URL to be used for this flow. This MUST be in the form of a URL.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The authorization URL to be used for this flow. This MUST be in the form of a URL. The OAuth2 standard requires the use of TLS.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: The authorization URL to be used for this flow. This MUST be in the form of a URL. The OAuth2 standard requires the use of TLS.
      - name: tokenUrl
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The token URL to be used for this flow. This MUST be in the form of a URL.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The token URL to be used for this flow. This MUST be in the form of a URL. The OAuth2 standard requires the use of TLS.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: The token URL to be used for this flow. This MUST be in the form of a URL. The OAuth2 standard requires the use of TLS.
      - name: refreshUrl
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The URL to be used for obtaining refresh tokens. This MUST be in the form of a URL.
          - version: '3.1'
            mainType: string
            parentType: null
            nameType: fixed
            richText: false
            isRequired: false
            canBeReference: false
            description: The URL to be used for obtaining refresh tokens. This MUST be in the form of a URL. The OAuth2 standard requires the use of TLS.
        mainType: string
        parentType: null
        nameType: fixed
        richText: false
        isRequired: false
        canBeReference: false
        description: The URL to be used for obtaining refresh tokens. This MUST be in the form of a URL. The OAuth2 standard requires the use of TLS.
      - name: scopes
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: string
            parentType: map
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The available scopes for the OAuth2 security scheme. A map between the scope name and a short description for it. The map MAY be empty.
          - version: '3.1'
            mainType: string
            parentType: map
            nameType: fixed
            richText: false
            isRequired: true
            canBeReference: false
            description: The available scopes for the OAuth2 security scheme. A map between the scope name and a short description for it. The map MAY be empty.
        mainType: string
        parentType: map
        nameType: fixed
        richText: false
        isRequired: true
        canBeReference: false
        description: The available scopes for the OAuth2 security scheme. A map between the scope name and a short description for it. The map MAY be empty.
      - name: ^x-
        versions:
          - '3.0'
          - '3.1'
        logs:
          - version: '3.0'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. The value can be <code>null</code>, a primitive, an array or an object. Can have any valid JSON format value.
          - version: '3.1'
            mainType: Any
            parentType: null
            nameType: patterned
            richText: false
            isRequired: false
            canBeReference: false
            description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
        mainType: Any
        parentType: null
        nameType: patterned
        richText: false
        isRequired: false
        canBeReference: false
        description: Allows extensions to the OpenAPI Schema. The field name MUST begin with <code>x-</code>, for example, <code>x-internal-id</code>. Field names beginning <code>x-oai-</code> and <code>x-oas-</code> are reserved for uses defined by the <a href="https://www.openapis.org/">OpenAPI Initiative</a>. The value can be <code>null</code>, a primitive, an array or an object.
